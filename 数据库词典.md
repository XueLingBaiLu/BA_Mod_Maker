# Broken Arrow 数据库词典 (Database Glossary)

本文档由 clean_baseline.json（解密后的干净数据库）自动生成，
覆盖全部 24 张表、所有字段、外键关系与枚举值，
并收纳数据结构、地址映射、常量与防崩溃铁律（原教程/技术总结内容全部并入本文档）。

## 数据总览

| 表名 | 中文 | 行数 | 说明 |
|---|---|---:|---|
| `Countries` | 阵营/国家 | 3 | 游戏阵营（俄罗斯、美国）及编辑器用的虚拟阵营，定义阵营点数上限与旗帜。 |
| `Units` | 单位 | 540 | 所有单位（步兵、载具、直升机、飞机、舰船）的核心属性表，是整个数据库的中心。 |
| `Weapons` | 武器 | 804 | 所有武器定义（枪械、导弹、火炮、炸弹等），不含弹药本身。 |
| `Ammunitions` | 弹药 | 630 | 武器的弹药/弹种定义（穿甲、高爆、导弹、炸弹等）及其弹道、毁伤参数。 |
| `Armors` | 装甲 | 319 | 装甲套装定义（正面/侧面/顶部/后部 的动能与破甲防护值、生命值）。 |
| `Mobility` | 机动性 | 266 | 机动性模板（履带/轮式/两栖/飞行），定义速度、转向、爬坡等。 |
| `FlyPresets` | 飞行预设 | 23 | 固定翼飞机/直升机的飞行包线预设（速度、俯仰/偏航、加力等）。 公式：前进速度(公里/小时) = MaxSpeed × 3.6；转弯半径(米) = 2 × MaxSpeed × (tan(MaxSpeedYaw×π/180)+1) ÷ tan(MaxSpeedYaw×π/180) |
| `Sensors` | 传感器 | 36 | 观瞄/雷达传感器模板（地面/低空/高空 视野距离）。 |
| `Turrets` | 炮塔 | 1055 | 炮塔/武器站定义（旋转范围、转速、是否全向旋转、父子炮塔）。 |
| `Abilities` | 能力 | 78 | 单位特殊能力（烟雾、APS 主动防御、雷达、诱饵、补给、潜行、激光指示等）。 |
| `Modifications` | 改装 | 615 | 单位的改装槽（装甲/武器/引擎/光学/能力/班组/变体），每个槽含若干选项。 |
| `Options` | 选项 | 2045 | 改装的具体选项，定义替换的装甲/机动/炮塔/能力及价格、贴图等。 |
| `Specializations` | 专精 | 13 | 师级专精（如 USMC、装甲旅、空降兵），定义卡组槽位数量与点数。 |
| `UnitAbilities` | 单位-能力关联 | 655 | 链接表：单位与能力（多对多）。 |
| `UnitPropulsions` | 单位-机动关联 | 646 | 链接表：单位与机动性模板。 |
| `UnitArmors` | 单位-装甲关联 | 711 | 链接表：单位与装甲套装。 |
| `SensorUnits` | 单位-传感器关联 | 609 | 链接表：单位与传感器。 |
| `TurretUnits` | 单位-炮塔关联 | 1597 | 链接表：单位与炮塔（含挂载顺序）。 |
| `TurretWeapons` | 炮塔-武器关联 | 1280 | 链接表：炮塔与武器（含武器通道与优先级、顺序）。 |
| `WeaponAmmunitions` | 武器-弹药关联 | 2358 | 链接表：武器、弹药与携带数量（含单位级覆盖）。 |
| `SquadMembers` | 班组成员 | 1134 | 步兵班组成员定义（模型、主武器、特殊武器、死亡优先级）。 |
| `SquadWeapons` | 班组武器 | 706 | 链接表：班组携带的武器（含顺序）。 |
| `SpecializationAvailabilities` | 专精-单位可用性 | 338 | 链接表：专精与单位的可用数量（按经验等级 XP0~XP3）。 |
| `TransportAvailabilities` | 运输可用性 | 227 | 链接表：可运输的单位（挂在专精可用性之下）。 |

## Countries（阵营/国家）

> 游戏阵营（俄罗斯、美国）及编辑器用的虚拟阵营，定义阵营点数上限与旗帜。 （行数：3）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `Name` | str | 名称：内部英文名（数据库内唯一标识，非显示名）。例：'BTR-82'、'M1A2 SEP v2 TUA'——用于表间引用和代码查找，玩家看到的是 HUDName。 |
| `UIName` | str | 界面名：界面显示名称（本地化键）。例：'Custom_Slot_Armor' 对应界面的装甲槽——指向本地化文本表的键，翻译后显示。 |
| `FlagFileName` | str | 旗帜文件名：旗帜图片资源名。例：Russia 用 'rus flag'——决定阵营界面显示的旗帜图标。 |
| `MaxPoints` | int | 点数上限：该阵营在卡组中的最大点数。例：Russia/USA 各 10000，编辑器阵营 800000——决定卡组能花费的总点数上限。 |
| `Hidden` | bool | 是否隐藏：是否在界面隐藏该阵营。例：编辑器阵营 Hidden=true 不显示给玩家——隐藏内部/测试阵营。 |
| `ContentMembership` | int | 内容归属：DLC/内容包归属。例：-1=本体，2=DLC2——决定阵营属于哪个内容包。 |

## Units（单位）

> 所有单位（步兵、载具、直升机、飞机、舰船）的核心属性表，是整个数据库的中心。 （行数：540）

| 字段 | 类型 | 含义 |
|---|---|---|
| `OriginalName` | unknown | 原始名称：原始名称（未使用，通常为空）。例：用于记录单位改名前的原名——保留的备份字段，不影响游戏。 |
| `OwnerInfantryID` | int | 所属步兵 ID：所属步兵小队单位 ID。例：载具设置 OwnerInfantryID 指向它携带的步兵班，0 表示无——用于把步兵小队挂到载具上。 |
| `OriginalCost` | int | 原始价格：原始价格（备份字段，通常为 0）。例：记录单位价格改动前的原值——备份字段，不影响实际价格。 |
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `DisplayInArmory` | bool | 在军械库显示：是否在军械库显示。例：true 的单位会出现在玩家可挑选的军械库列表中——false 则隐藏（如测试/僵尸单位）。 |
| `IsUnitModification` | bool | 是否改装单位：是否为改装派生单位。例：改装选项替换出的衍生单位会标记 true——区分原始单位与改装生成的变体。 |
| `HUDName` | str | 界面显示名：游戏 HUD 中显示的名称。例：武器 HUDName='2А72 30mm' 显示在武器栏——决定玩家在界面看到的名称。 |
| `Name` | str | 名称：内部英文名（数据库内唯一标识，非显示名）。例：'BTR-82'、'M1A2 SEP v2 TUA'——用于表间引用和代码查找，玩家看到的是 HUDName。 |
| `CountryId` | int | 阵营 ID：阵营 ID（外键 → Countries）。例：1=Russia、2=USA——把单位/专精归属到对应阵营。 |
| `Description` | unknown | 描述：单位描述文本。例：单位的背景介绍/说明文字，显示在单位详情界面——用于给玩家介绍单位。 |
| `ModelFileName` | str | 模型文件名：3D 模型资源名。例：BTR-82 用 'RU_BTR82A' 模型——修改它可替换单位在游戏中的 3D 外观。 |
| `PortraitFileName` | str | 头像路径：头像图片资源路径。例：'RU\BTR_82A\BTR_82A'——决定单位在界面的头像图片。 |
| `ThumbnailFileName` | str | 缩略图：缩略图资源名。例：'RU_BTR82-Label'——决定单位在列表/卡组里的缩略图。 |
| `AudioPreset` | str | 音效预设：音效预设资源名。例：M1A2 用 'UNIT_SOUND_PRESET_ABRAMS'——决定单位开火/移动/引擎的声音。 |
| `Cost` | int | 价格：购买所需点数。例：BTR-82 只要 55 点，可大量部署；M1A2 SEP v2 高达 300 点，单辆就占掉大量卡组预算——价格决定单位能否频繁登场，高价值单位通常更强但更难量产。 |
| `UpkeepMultiplier` | float | 维护倍率：维护费倍率。例：多数单位 1.0，特种/稀有单位更高——倍率越高，维持该单位的持续开销越大。 |
| `TimeModifierRefund` | float | 撤退返还修正：单位：秒。正数＝增加 N 秒，负数＝减少 N 秒。直接影响「毫发无损」「弹药耗尽」的回转时间，并以 2.5 倍影响「阵亡」回转时间。 |
| `TimeModifierResurrect` | float | 复活时间修正：单位：秒。正数＝增加 N 秒，负数＝减少 N 秒。直接影响「阵亡」回转时间。 |
| `Type` | int | 单位大类（引擎级）：引擎级位掩码：2 步兵 / 4 载具 / 8 直升机 / 16 飞机 / 32 舰船 / 128 弹丸 / 256 反辐射导弹 / 512 巡航导弹 / 1024 弹道导弹。决定单位走哪套引擎分支（出生时只有 Type=2 或 4 才会挂 AI 目标组件）。注意：它与「卡组槽位」（CategoryType）和「战术角色」（Role）是三件不同的事。 |
| `CategoryType` | int | 卡组槽位类别：取值：0 侦察 / 1 步兵 / 2 战斗 / 3 支援 / 4 后勤(舰船) / 5 直升机 / 6 飞机。卡组编辑器的可用槽位数量按它索引（DeckHelper.GetAvailablesSlots）。与 Role 无关——同一 Role 的单位（例如主战坦克）可以分布在多个槽位类别里。 |
| `Role` | int | 角色（战术兵种）：单位的战术兵种枚举 UnitRole（比 Type 大类更细的细分）。真实作用有三处实证：① 出生时决定「哪把武器是主武器」——ATGM 步兵(35)只认 ATGM/ATGMHE 类武器，炮兵(130-133)只认榴弹炮/迫击炮/火箭炮与巡航/弹道导弹，防空(15/16/34)只认 MANPAD/SAM/高射炮，其它 Role 全部武器都算主武器；主武器弹药打空会触发语音与单位标签警示。② AI 火力任务——AI 火炮用发射单位的 Role 查 AiConfig.TargetingPresetOverrides（冷却/最小目标群造价/首选任务时长/目标 Role 黑名单），未命中用默认预设；每个步兵/载具出生时还会把 Role+Cost 写进 AiTargetComponent 供 AI 分组。③ 战报与勋章——按 Role 统计击杀，对应 7 枚勋章（坦克/战车/步兵/火炮/直升机/防空/飞机）；任务 Lua 脚本也能读 LuaUnit.UnitRole。不影响卡组槽位（看 CategoryType）与可打击目标（看武器/弹药）。填枚举外的值不会崩溃，但一律走兜底分支（所有武器都算主武器 + AI 默认预设）。 |
| `Length` | float | 长度：单位长度（米）。例：BTR-82 长 7 米——尺寸影响被弹面积（越大越容易被命中）和道路通过性。 |
| `Width` | float | 宽度：单位宽度（米）。例：BTR-82 宽 2.8 米——配合长度决定单位体型和碰撞体积。 |
| `Height` | float | 高度：单位高度（米）。例：BTR-82 高 2.5 米——影响能否被低矮掩体遮挡、被雷达/观瞄发现的高度因素。 |
| `Weight` | int | 重量：单位重量（千克）。例：BTR-82 约 15 吨，M1A2 约 64.5 吨——重量影响能否被运输机/直升机吊运、压桥等。注意：步兵单位界面显示重量 = 125 × 小队人数（kg），不读取此字段。 |
| `Stealth` | float | 隐蔽性：隐蔽性系数（越小越难被发现）。例：M1A2 坦克 0.8，BTR-82 1.0——低值单位更难被敌方观瞄/雷达发现，适合伏击与渗透，先手打击。界面显示隐蔽值 = 1 ÷ 此系数（0.8 → 1.25，1.0 → 1.0，保留两位小数）。 |
| `InfantrySlots` | int | 步兵位：可搭载步兵数量。例：BTR-82 有 8 个步兵位，能装载一整班步兵随车机动；主战坦克 0 个——决定载具能否运输步兵及其规模。 |
| `MaxStress` | float | 最大压力：最大压力（士气）值。例：单位 MaxStress=1000，被持续炮击/压制时压力累积，达到上限即惊慌/溃散——值越高越能扛住压制。 |
| `WaterDiveOffset` | float | 水中下潜偏移：水中下潜深度偏移。例：两栖/潜艇类单位有下潜偏移，普通载具为 0——决定单位在水中的吃水/下潜深度。 |
| `ContentMembership` | int | 内容归属：DLC/内容包归属。例：-1=本体单位，2=DLC2 单位——决定单位属于哪个内容包，未购买对应 DLC 时不可用。 |
| `CurrentStealth` | float | 当前隐蔽值：当前隐蔽值（运行期字段）。例：运行时记录的实时隐蔽状态，通常由 Stealth 初始化——游戏运行时的动态值，编辑时一般不动。 |

## Weapons（武器）

> 所有武器定义（枪械、导弹、火炮、炸弹等），不含弹药本身。 （行数：804）

| 字段 | 类型 | 含义 |
|---|---|---|
| `WeaponChannel` | int | 武器通道：武器通道编号（多武器分组）。例：同一炮塔的多个武器分到不同通道，按通道交替/并行射击——用于分组管理武器射击。 |
| `WeaponPriority` | int | 武器优先级：武器优先级（数值越大越优先）。例：优先值高的武器先开火——决定多武器时的射击顺序。 |
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `Name` | str | 名称：内部英文名（数据库内唯一标识，非显示名）。例：'BTR-82'、'M1A2 SEP v2 TUA'——用于表间引用和代码查找，玩家看到的是 HUDName。 |
| `HUDName` | str | 界面显示名：游戏 HUD 中显示的名称。例：武器 HUDName='2А72 30mm' 显示在武器栏——决定玩家在界面看到的名称。 |
| `Type` | int | 武器类型：武器类型（见枚举表）。例：1=步枪(M27)、80=反坦克导弹(TOW)、110=主炮(2A46M)、114=机关炮(2A72)、160=炸弹——决定武器的攻击方式和用途。 |
| `HUDIcon` | str | 界面图标：武器 HUD 图标资源名。例：'2A72' 是机关炮的图标——决定武器栏显示的图标。 |
| `AudioPreset` | str | 音效预设：音效预设资源名。例：M1A2 用 'UNIT_SOUND_PRESET_ABRAMS'——决定单位开火/移动/引擎的声音。 |
| `ModelFileName` | str | 模型文件名：3D 模型资源名。例：BTR-82 用 'RU_BTR82A' 模型——修改它可替换单位在游戏中的 3D 外观。 |
| `AutoLoaded` | bool | 自动装填：是否自动装填。例：自动装弹机让主炮无需人工装填，射速稳定——auto-loaded 武器装填更快更省人力。 |
| `Silent` | bool | 是否消音：是否消音。例：消音步枪/冲锋枪开火不暴露位置——silent 武器适合偷袭/渗透，不易被敌人发现。 |
| `HasPriorityOnChassis` | bool | 底盘优先级：是否在底盘上有优先级。例：true 的武器优先随底盘（载具）主控——决定武器是否绑定到底盘主控逻辑。 |
| `CQC` | bool | 近战：是否近战武器。例：M27 步枪 CQC=true，适合近距离巷战；机枪/狙击枪 false——近战武器在贴身距离更有效。 |
| `IsRadarDependent` | bool | 依赖雷达：是否依赖雷达引导。例：雷达制导导弹 true，需雷达锁定目标——决定武器是否需要雷达支持。 |
| `IsVerticalLauncher` | bool | 垂直发射：是否垂直发射装置。例：垂直发射系统（VLS）true，导弹垂直发射——决定武器的发射方式。 |
| `IsLowAltDirectWeapon` | bool | 低空直射：是否低空直射武器。例：低空直射武器用于低空交战——标记武器适合低空目标。 |
| `IsHighAltDirectWeapon` | bool | 高空直射：是否高空直射武器。例：高空直射武器用于高空交战——标记武器适合高空目标。 |
| `IsUnderbarrel` | bool | 下挂：是否为下挂武器。例：枪挂榴弹发射器是下挂武器——标记武器挂载在另一武器下方。 |
| `AimTimeMin` | float | 瞄准时间下限：瞄准时间下限（秒）。例：步枪 1 秒即可瞄准，榴弹炮需 16 秒——瞄准越快，越能快速接敌/切换目标。 |
| `AimTimeMax` | float | 瞄准时间上限：瞄准时间上限（秒）。例：与下限共同决定瞄准耗时，高值意味着武器需要长时间稳定才能精确射击——影响反应速度。 |
| `MultiTargetTracking` | int | 多目标跟踪：可同时跟踪目标数。例：多数武器 1（一次锁一个），高级防空 4+（同时跟踪多个目标）——决定多目标接战能力。 |
| `SimultaneousTracking` | int | 同时跟踪数：可同时攻击目标数。例：多数武器 1（一次打一个），相控阵防空可同时攻击多个——决定能同时向几个目标开火。 |
| `CanShootOnTheMove` | bool | 移动射击：是否可移动中射击。例：步兵步枪 true，自行榴弹炮 false——true 的武器能在行进间开火，false 需停车才能射击，影响机动战术。 |
| `StabilizerQuality` | float | 稳定器质量：稳定器质量系数。例：稳定器好的武器移动中射击更准——值越高，移动/行进间射击精度越高。 |
| `FlashPerShot` | float | 每发火光：每发枪口火光系数。例：火光大的武器开火时更显眼，容易被发现——值越低越隐蔽。 |
| `CanBeMerged` | bool | 可合并：是否可合并。例：同种武器的弹药可合并成一个弹药条，简化弹药管理——用于把多个相同武器挂载的弹药汇总。 |
| `LowerVerticalAngle` | int | 下俯角：向下俯仰最大角度。例：坦克炮 -10° 可压低射击——决定武器能向下压多大角度。 |
| `UpperVerticalAngles` | int | 上仰角：向上仰角最大角度。例：坦克炮 +20° 可抬升射击——决定武器能向上抬多大角度。 |
| `VerticalRotationSpeed` | int | 俯仰转速：俯仰旋转速度。例：炮管俯仰速度决定跟瞄快慢——影响对高低方向目标的追踪。 |
| `MagazineSize` | int | 弹匣容量：弹匣/弹容量。例：M27 步枪 30 发，2A72 机关炮 50 发——决定一次装填能打多少发，弹容量越大持续火力越久、装填频率越低。 |
| `MagazineReloadTimeMin` | float | 装填时间下限：装填时间下限（秒）。例：M27 步枪装填 4 秒，榴弹炮 7 秒——装填期间无法开火，值越低火力真空期越短。 |
| `MagazineReloadTimeMax` | float | 装填时间上限：装填时间上限（秒）。例：与下限共同决定装填耗时范围，通常取最大值估算最坏情况——决定持续火力节奏。 |
| `ShotsPerBurstMin` | int | 每波射击下限：每波射击数下限。例：步枪单发点射(1)，机关炮 4 发短点射——决定一次扣扳机打出多少发。 |
| `ShotsPerBurstMax` | int | 每波射击上限：每波射击数上限。例：机关炮最多 8 发连射——决定最大点射长度，影响弹药消耗和压制效果。 |
| `TimeBetweenBurstsMin` | float | 波次间隔下限：波次间隔下限（秒）。例：步枪点射间隔 4 秒，机关炮 2.2 秒——间隔越短，火力越连续。 |
| `TimeBetweenBurstsMax` | float | 波次间隔上限：波次间隔上限（秒）。例：与下限共同决定射击节奏——高值武器打完一波后要休息更久。 |
| `TimeBetweenShotsInBurst` | float | 波内射击间隔：波内每发间隔（秒）。例：机关炮波内 0.55 秒/发，即每分钟约 109 发——决定连射时的射速。 |
| `ShortGroundAttackBurst` | int | 对地短波射击：对地短波射击数。例：短波射击打 3 发——决定对地短点射的长度。 |
| `NormalGroundAttackBurst` | int | 对地常规波射击：对地常规波射击数。例：常规对地射击打 5 发——决定对地常规点射的长度。 |
| `LongGroundAttackBurst` | int | 对地长波射击：对地长波射击数。例：长波射击打 9 发，用于持续压制——决定对地长点射的长度。 |

## Ammunitions（弹药）

> 武器的弹药/弹种定义（穿甲、高爆、导弹、炸弹等）及其弹道、毁伤参数。 （行数：630）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `Name` | str | 名称：内部英文名（数据库内唯一标识，非显示名）。例：'BTR-82'、'M1A2 SEP v2 TUA'——用于表间引用和代码查找，玩家看到的是 HUDName。 |
| `HUDName` | str | 界面显示名：游戏 HUD 中显示的名称。例：武器 HUDName='2А72 30mm' 显示在武器栏——决定玩家在界面看到的名称。 |
| `HUDIcon` | str | 界面图标：弹药 HUD 图标资源名。例：'AMMO_US_TANK_120_AP' 是 M829A4 的图标——决定弹药栏显示的图标。 |
| `HUDMultiplier` | int | 界面倍率：HUD 数量倍率。例：HUDMultiplier=3 表示界面上 1 格等于 3 发——用于简化弹药数量显示。 |
| `UiOptions` | int | 界面选项：界面选项位标志。例：标志控制弹药在界面的显示方式（如是否显示数量）——影响 UI 表现。 |
| `ModelFileName` | str | 模型文件名：3D 模型资源名。例：BTR-82 用 'RU_BTR82A' 模型——修改它可替换单位在游戏中的 3D 外观。 |
| `AudioPreset` | str | 音效预设：音效预设资源名。例：M1A2 用 'UNIT_SOUND_PRESET_ABRAMS'——决定单位开火/移动/引擎的声音。 |
| `FXPreset` | str | 特效预设：命中/爆炸特效预设资源名。例：'AMMUNITION_VFX_PRESET_ROUND_120mm_AP' 是 120mm 穿甲弹的特效——决定命中/爆炸的视觉表现。 |
| `VfxCountPerShot` | int | 每发特效数：每发弹产生的特效数量。例：多发特效让爆炸更壮观——决定单发弹播放多少个特效。 |
| `VfxCountPerShotMax` | int | 每发特效数上限：每发弹特效数量上限（0=不限制）。例：限制特效数量防止性能下降——控制单发弹特效的封顶数量。 |
| `Airburst` | bool | 是否空爆：是否空爆。例：无线电近炸引信弹药在目标上空爆炸，把破片洒向掩体后的敌人——空爆专门克制躲掩体的步兵。 |
| `SupplyCost` | float | 补给消耗：补给该弹的消耗。例：坦克穿甲弹每发补给消耗 20，机关炮弹仅 1.2——高价值弹药补给慢/贵，决定持续作战的补给压力。 |
| `ResupplyTime` | float | 补给时间：单位：秒。该弹药的补给耗时。直接影响「弹药耗尽」回转时间，并以 2.5 倍影响「阵亡」回转时间。 |
| `Damage` | float | 伤害：单发伤害值。例：M829A4 穿甲弹 Damage=10，命中主战坦克正面即可重创甚至一发击毁；30mm 机关炮弹 Damage=1.5，需连续命中才能杀伤轻甲目标——数值越高，对目标杀伤越快。 |
| `StressDamage` | float | 压力伤害：对目标士气/压力的伤害。例：120mm 穿甲弹 StressDamage=120，命中后会严重打击敌军士气，使步兵班组惊慌甚至溃散；30mm 弹仅 30——高值用于压制、驱散敌军，而非物理击杀。 |
| `DamageOverTimeDuration` | float | 持续伤害时间：持续伤害时长（如燃烧）。例：燃烧弹有 DoT 持续时间，命中后目标持续掉血数秒——用于白磷弹/燃烧弹，持续压制步兵和轻甲。 |
| `IgnoreCover` | float | 无视掩体：无视掩体的比例。例：HEAT 弹 IgnoreCover=0.2，能部分无视掩体杀伤掩体后的目标——值越高，越能打穿掩体/建筑后的敌人。 |
| `TargetType` | int | 目标类型掩码：可攻击目标类型位掩码（按位相加，完整位表见下方枚举值）。例：Tor 防空导弹=1944（8直升机+16飞机+128反辐射+256巡航+512弹道+1024制导），能打直升机、固定翼飞机并拦截四类导弹；M829A4 穿甲弹=36（4载具+32舰船），只能打装甲和舰船——各位相加决定该弹能锁定哪些目标。 |
| `ArmorTargeted` | int | 装甲目标类型：针对的装甲类型：0=无 1=动能穿甲 2=破甲/高爆。例：M829A4 是动能弹(1)，靠高速硬芯穿透装甲；M830A1 是破甲弹(2)，靠金属射流——动能弹怕反应装甲/倾斜角，破甲弹怕间隙装甲。 |
| `PenetrationAtMinRange` | float | 近距离穿深：最近距离穿透值（毫米）。例：M829A4 穿甲弹近距穿深 840mm，可正面击穿绝大多数主战坦克；HEAT 弹仅 400mm，对重甲/反应装甲可能无法击穿——决定该弹能否打穿目标装甲。 |
| `PenetrationAtGroundRange` | float | 地面射程穿深：地面射程处穿透值（毫米），通常低于近距离穿深。例：M829A4 在 700m 处仍保留 525mm 穿深，远距离依然能击穿坦克；30mm 弹仅剩 50mm——体现穿甲能力随距离的衰减。 |
| `NoDamageFalloff` | bool | 无伤害衰减：伤害是否不随距离衰减。例：true 的弹药全射程伤害恒定——决定伤害是否随距离递减。 |
| `TopArmorAttack` | bool | 顶部攻击：是否顶部攻击。例：标枪反坦克导弹走攻顶弹道，命中坦克最薄的顶部装甲——顶部攻击能绕开厚重正面装甲，专门克制重甲目标。 |
| `HealthAOERadius` | float | 生命范围伤害半径：范围伤害（生命）半径。例：155mm 高爆弹有 AoE 半径，落地爆炸能同时杀伤半径内多个单位——AoE 越大，一次射击覆盖的目标越多。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `StressAOERadius` | float | 压力范围伤害半径：范围伤害（压力）半径。例：高爆弹在半径内对敌军造成士气打击——决定范围压制效果的范围。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `OverpressureRadius` | float | 超压半径：超压伤害半径。例：大口径高爆弹产生超压，即使未直接命中也能靠冲击波杀伤附近无装甲目标——用于清理聚集的步兵。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `RadioFuseDistance` | float | 无线电引信距离：无线电近炸引信距离。例：近炸引信在距目标 7 米时引爆，无需直接命中即可杀伤——用于打飞机/直升机等高速机动目标。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `CriticMultiplier` | float | 暴击倍率：暴击伤害倍率。例：狙击弹/穿甲弹 CriticMultiplier=5，命中弱点（如弹药架、发动机）时伤害×5——高暴击倍率用于精准打击弱点，一发致命。 |
| `CanBeIntercepted` | bool | 可被拦截：是否可被主动防御拦截。例：导弹/火箭弹可被 APS 拦截；高速穿甲弹不可拦截——设为 true 的弹药会被敌方主动防御系统击落。 |
| `CanBeTargeted` | bool | 可被锁定：是否可被防空/拦截系统锁定。例：飞机、导弹可被雷达锁定；炮弹不能——决定该弹药能否成为防空/反导系统的目标。 |
| `TrajectoryType` | int | 弹道类型：弹道类型。例：坦克炮弹是直射(10)，飞直线、命中快；榴弹炮是曲射(20)，走抛物线可越过掩体；导弹(110/200/300)会跟踪目标——决定弹药怎么飞、能否越过障碍。 |
| `MinimalRange` | float | 最小射程：最小射程（米）。例：远程导弹有最小射程（如几十米），目标贴得太近反而无法开火——形成火力死区，近距离需要副武器。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `GroundRange` | float | 地面射程：对地面目标射程。例：M829A4 地面射程值 700（游戏内 1400m），超出则无法攻击——决定该武器的有效交战距离，影响阵地选择和先手。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `LowAltRange` | float | 低空射程：对低空目标射程。例：地面防空弹有低空射程，能打低空飞行的直升机/无人机；纯反坦克弹为 0——决定能否拦截低空飞行器。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `HighAltRange` | float | 高空射程：对高空目标射程。例：远程防空导弹有高空射程，能打高空飞行的飞机；近程防空/地面弹药为 0——决定能否拦截高空目标。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `NonSeadProjectileRangeOverride` | float | 非压制射程覆盖：非 SEAD 弹道射程覆盖。例：普通弹的射程覆盖值，0=用默认射程——用于特殊弹道单独调射程。 |
| `SeadProjectileRangeOverride` | float | 压制射程覆盖：SEAD（反辐射）弹道射程覆盖。例：反辐射弹的射程覆盖值——反辐射导弹有独立的射程设定。 |
| `OverflyHeight` | int | 飞越高度：攻顶导弹在目标上方的飞越高度（米）。例：TOW-2B/RBS-56 为 2，飞到坦克上方 2 米处向下引爆、攻击最薄的顶部装甲——攻顶弹药专用，绕开厚重正面装甲。 |
| `MuzzleVelocity` | float | 初速：炮口/发射初速（米/秒）。例：M829A4 初速 1050 m/s，飞得快、弹道平直、难躲避；HEAT 弹仅 550 m/s——初速越高，命中率越高、飞行时间越短。 |
| `MaxSpeed` | float | 最大速度：飞行最大速度。例：穿甲弹 MaxSpeed 高（配合初速），导弹飞行速度决定其被拦截的难度——速度越快越难被拦截。 |
| `Acceleration` | float | 加速度：飞行加速度。例：导弹发射后持续加速（Acceleration>0），逐渐达到最大速度——加速度决定导弹多快进入高速段。 |
| `DispersionHorizontalRadius` | float | 水平散布半径：水平散布半径。例：机关炮散布较大，远距离弹着点分散——散布越大，远距离命中率越低，适合近距离压制而非精确点杀。 |
| `DispersionVerticalRadius` | float | 垂直散布半径：垂直散布半径。例：与水平散布共同决定弹着点椭圆——数值越小，弹着越集中，精准度越高。 |
| `DispersionMinimal` | float | 最小散布：最小散布值。例：武器在最佳条件下的最小散布——决定该弹药能达到的最高精度下限。 |
| `LaunchMarginAngle` | float | 发射余量角：发射允许偏离角（度）。例：TOW-2A 为 30°，即导弹与目标指向夹角不超过 30° 才能发射/锁定；Arkan 仅 2°（波束制导需几乎正对目标）——决定发射时的角度容差。 |
| `LoftAngle` | int | 高抛角：高抛弹道角度。例：某些导弹先以高角度爬升再俯冲——决定高抛弹道的爬升角。 |
| `LoftHeight` | int | 高抛高度：高抛弹道高度。例：导弹高抛爬升到设定高度后俯冲——决定高抛弹道的顶点高度。 |
| `AimTimeMinOverride` | float | 瞄准时间下限覆盖：瞄准时间下限覆盖。例：覆盖武器默认的瞄准下限——用于特殊弹药单独调瞄准时间。 |
| `AimTimeMaxOverride` | float | 瞄准时间上限覆盖：瞄准时间上限覆盖。例：覆盖武器默认的瞄准上限——用于特殊弹药单独调瞄准时间。 |
| `GenerateSmoke` | bool | 生成烟雾：命中后是否生成烟雾。例：白磷弹命中后生成烟雾——用于遮蔽视线和持续燃烧。 |
| `SmokeRadius` | float | 烟雾半径：烟雾半径。例：命中后烟幕覆盖的半径——决定烟幕遮蔽范围。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `SmokeDuration` | float | 烟雾持续时间：烟雾持续时间。例：烟幕持续数秒后消散——决定烟幕遮蔽的时长。 |
| `SmokeFadeDuration` | float | 烟雾消散时间：烟雾消散时间。例：烟幕逐渐消散的过渡时长——决定烟幕淡出的平滑度。 |
| `Seeker` | int | 导引头类型：导引头/制导方式。例：0=无制导（炮弹）；10=红外(Stinger、AIM-9 发射后不管)；50=末端制导(地狱火、标枪)；100=线导/半主动(TOW，需持续瞄准)；200=反辐射(Kh-31，追雷达信号)——决定导弹如何追踪目标。 |
| `LaserGuided` | bool | 是否激光制导：是否激光制导。例：激光制导炸弹/导弹需友军激光照射目标才能命中——需要激光指示器配合，但精度极高。 |
| `SeekerAngle` | float | 导引头视场角：导引头视场角。例：红外导弹导引头视角有限，目标需进入视场才能锁定——视场角越大，越容易捕捉目标。 |
| `MaxSeekerDistance` | float | 导引头最大距离：导引头锁定最大距离。例：导弹需在锁定距离内才能捕获目标——超过该距离无法发射/追踪，决定导弹的有效接战距离。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `CanReaquire` | bool | 可重新锁定：是否可重新锁定目标。例：true 的导弹丢失目标后能重新捕获——提升导弹追踪的可靠性。 |
| `AimStartDelay` | float | 瞄准启动延迟：制导/瞄准启动延迟（秒）。例：S-300V 仅 0.075 秒、AMRAAM 0.5 秒、战斧 1 秒——导弹发射后延迟这么久才开始转向/追踪目标，值越小响应越快。 |
| `MainEngineIgnitionDelay` | float | 主发动机点火延迟：主发动机点火延迟（秒）。例：Tor 仅 0.15 秒、战斧 1 秒——导弹离开发射架后延迟这么久主发动机才点火，期间靠初始推力/惯性滑行。 |
| `RotationSpeed` | float | 旋转速度：导弹转弯速率/弹道调整速度（度/秒）。例：地狱火=160，能急剧转弯追击机动目标；巡航导弹=45，机动较缓；制导火箭弹=20~30，仅做小幅末端修正；无制导炮弹=0——决定导弹追踪目标时能多快改变飞行方向，值越高越能咬住高速/机动目标。 |
| `BurnTime` | float | 燃烧时间：发动机燃烧时间。例：导弹发动机燃烧 3 秒，期间持续加速，熄火后靠惯性滑翔——燃烧时间决定导弹的加速段和最大射程。 |
| `PreEgnitionRotationSpeed` | float | 点火前旋转速度：点火前弹道调整速度（度/秒）。例：垂直发射的 Tor/S-300 为 600，发射后需在点火前迅速转向目标方位；Pantsir/通古斯卡 150；R-60/R-73 空空弹 90~120；巡航导弹 30——决定导弹在主发动机点火前能多快调整飞行方向。 |
| `PreventReloadWhileTracked` | bool | 跟踪时禁止装填：跟踪目标时是否禁止装填。例：true 时跟踪期间不装填，防止中断跟踪——控制装填时机。 |
| `StripingIgnore` | bool | 忽略条纹：忽略条纹（穿甲）标志。例：APFSDS 尾翼稳定穿甲弹的标志——内部穿甲逻辑的标记位。 |
| `IsTopArmorArmorAttack` | bool | 顶部装甲攻击：是否为顶部装甲攻击。例：攻顶弹药 true，命中目标最薄的顶部——顶部攻击绕开厚重正面装甲。 |

## Armors（装甲）

> 装甲套装定义（正面/侧面/顶部/后部 的动能与破甲防护值、生命值）。 （行数：319）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `IsDefault` | bool | 是否默认：是否为该表的默认选项。例：IsDefault=true 的装甲/传感器是单位的初始配置，无需额外选择即可使用——标记基础项。 |
| `Name` | str | 名称：内部英文名（数据库内唯一标识，非显示名）。例：'BTR-82'、'M1A2 SEP v2 TUA'——用于表间引用和代码查找，玩家看到的是 HUDName。 |
| `ModelFileName` | str | 模型文件名：3D 模型资源名。例：BTR-82 用 'RU_BTR82A' 模型——修改它可替换单位在游戏中的 3D 外观。 |
| `ArmorValue` | int | 装甲值：装甲综合数值（部分单位使用的简化护甲）。例：多数为 0，靠下方 Heat/Kin 六向值计算——作为简化的装甲强度参考。 |
| `MaxHealthPoints` | int | 最大生命值：最大生命值（HP）。例：BTR-80 装甲 14 HP，被打光 HP 即被摧毁——HP 越高越耐打，能承受更多命中。 |
| `HeatArmorFront` | int | 正面破甲防护：正面破甲（化学能）防护。例：BMP-3 正面 100，能扛住大部分破甲弹/反坦克导弹；BTR-80 仅 30——决定正面对 HEAT/化学能弹药的抗性。 |
| `HeatArmorRear` | int | 后部破甲防护：后部破甲防护。例：后部通常远低于正面（BMP-3 后部 40）——从背后攻击更容易击穿，鼓励绕后。 |
| `HeatArmorSides` | int | 侧面破甲防护：侧面破甲防护。例：BMP-3 侧面 60，介于正面与后部之间——侧袭比正面有效、比背后难。 |
| `HeatArmorTop` | int | 顶部破甲防护：顶部破甲防护。例：顶部最薄（BMP-3 顶部 40），是攻顶导弹/迫击炮的弱点——顶部攻击最容易击穿。 |
| `KinArmorFront` | int | 正面动能防护：正面动能（穿甲）防护。例：BMP-3 正面 100，可挡住动能穿甲弹；BTR-80 仅 30——决定正面对 APFSDS 等动能弹的抗性。 |
| `KinArmorRear` | int | 后部动能防护：后部动能防护。例：后部动能防护远低于正面，被绕后时穿甲弹极易击穿——同 HeatRear，鼓励打背后。 |
| `KinArmorSides` | int | 侧面动能防护：侧面动能防护。例：侧面介于正面与后部之间，侧袭穿甲弹比正面更容易穿透。 |
| `KinArmorTop` | int | 顶部动能防护：顶部动能防护。例：顶部最薄，是攻顶弹药和俯冲攻击的弱点——顶部动能防护低意味着顶部攻击极具威胁。 |

## Mobility（机动性）

> 机动性模板（履带/轮式/两栖/飞行），定义速度、转向、爬坡等。 （行数：266）

| 字段 | 类型 | 含义 |
|---|---|---|
| `FlyPreset` | unknown | 飞行预设(未用)：飞行预设（此字段未使用，改用 FlyPresetId）。例：空值，实际用 FlyPresetId 关联——保留的旧字段，忽略即可。 |
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `Name` | str | 名称：内部英文名（数据库内唯一标识，非显示名）。例：'BTR-82'、'M1A2 SEP v2 TUA'——用于表间引用和代码查找，玩家看到的是 HUDName。 |
| `ModelFileName` | str | 模型文件名：3D 模型资源名。例：BTR-82 用 'RU_BTR82A' 模型——修改它可替换单位在游戏中的 3D 外观。 |
| `IsDefault` | bool | 是否默认：是否为该表的默认选项。例：IsDefault=true 的装甲/传感器是单位的初始配置，无需额外选择即可使用——标记基础项。 |
| `IsAmphibious` | bool | 是否两栖：是否两栖。例：BMP-3 为 true，能直接渡河；BTR-82（NonAmph）为 false，遇水受阻——决定单位能否自行渡水。 |
| `IsAirDroppable` | bool | 是否可空投：是否可空投。例：空降单位 true，可由运输机空投到前线——决定单位能否通过空投部署。 |
| `Weight` | int | 重量：机动模板重量。例：与单位重量配合，决定能否被直升机吊运（受 HeavyLiftWeight 限制）——影响空运能力。 |
| `HeavyLiftWeight` | int | 重型吊运重量：直升机重型吊运重量。例：重型运输直升机有较大的吊运上限，能吊运轻装甲单位——决定该机动模板能否被吊运以及吊运多重的单位。 |
| `TurnRate` | float | 转向速率：转向速率（度/秒）。例：BTR-82 转向 80 度/秒，比 M1A2 的 60 更快——决定单位转弯的灵活度，影响绕侧和规避。 |
| `Acceleration` | float | 加速度：加速度。例：数值越高，单位从静止到极速越快——影响起步、追击和规避的响应速度。 |
| `MaxCrossCountrySpeed` | float | 越野速度：越野最大速度。例：M1A2 越野 55，略低于公路速度——决定单位在无路地形上的机动速度，履带车越野更强。 |
| `MaxSpeedRoad` | float | 公路速度：公路最大速度。例：BTR-82 轮式 100 速度，M1A2 履带 70——公路速度决定单位在道路上的机动速度，轮式通常更快。 |
| `MaxSpeedReverse` | float | 倒车速度：倒车最大速度。例：M1A2 倒车 35——决定单位后退撤离的速度，倒车快更利于脱离战斗。 |
| `MaxSpeedWater` | float | 水上速度：水上最大速度。例：两栖 BMP-3 水上 20，非两栖单位通常也有该值但需 IsAmphibious=true 才能用——决定水中机动速度。 |
| `Agility` | float | 敏捷：敏捷系数。例：数值越高单位转向/机动越灵活——影响单位的整体机动表现。 |
| `ClimbRate` | float | 爬坡率：爬坡率。例：履带车爬坡率高，能上陡坡；轮式车较低——决定单位能爬多陡的坡，影响山地机动。 |
| `IsChangeAltitude` | bool | 是否可升降：飞行器是否可改变高度。例：直升机 true，可升降调整高度；固定翼 false——决定飞行器能否主动变高。 |
| `LoiteringTime` | float | 盘旋时间：盘旋/滞空时间。例：无人机/直升机有滞空时间，决定其能在空中停留多久再返航——影响持续侦察/压制。 |
| `IsAfterburner` | bool | 是否加力：是否有加力燃烧室。例：超音速飞机 true，可开加力短时提速——决定飞行器能否加力冲刺。 |
| `AfterBurningLoiteringTime` | float | 加力盘旋时间：加力时盘旋时间。例：开加力时能盘旋的时长（加力耗油，时间更短）——决定加力滞空时间。 |
| `FlyPresetId` | int | 飞行预设 ID：飞行预设 ID（外键 → FlyPresets）。例：MiG-35 的机动性 FlyPresetId=19（Fighter-Top）——把机动模板关联到具体飞行包线。 |

## FlyPresets（飞行预设）

> 固定翼飞机/直升机的飞行包线预设（速度、俯仰/偏航、加力等）。 公式：前进速度(公里/小时) = MaxSpeed × 3.6；转弯半径(米) = 2 × MaxSpeed × (tan(MaxSpeedYaw×π/180)+1) ÷ tan(MaxSpeedYaw×π/180) （行数：23）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `Name` | str | 名称：内部英文名（数据库内唯一标识，非显示名）。例：'BTR-82'、'M1A2 SEP v2 TUA'——用于表间引用和代码查找，玩家看到的是 HUDName。 |
| `LowAltitudeIsDefault` | bool | 低空为默认：低空为默认。例：默认低空飞行的飞机更隐蔽，但易受近程防空威胁——决定飞机的默认巡航高度。 |
| `OverrideAltitude` | bool | 覆盖高度：是否覆盖默认飞行高度。例：某些飞机强制在特定高度飞行——用于模拟高空/低空突防的固定高度。 |
| `OverrideAltitideRatio` | float | 覆盖高度比：覆盖飞行高度的比例。例：OverrideAltitude=true 时按此比例调整高度——决定强制高度的缩放。 |
| `MinSpeed` | float | 最小速度：最小飞行速度。例：MiG-35 最低 175，低于此速度会失速——决定飞机能飞多慢，影响低速机动和着陆。 |
| `CornerSpeed` | float | 角速度点：角速度点（最佳转弯速度）。例：MiG-35 的 CornerSpeed=220，在此速度转弯效率最高——飞机在这个速度盘旋最灵活。 |
| `MaxSpeed` | float | 最大速度：最大飞行速度（米/秒）。例：MiG-35 MaxSpeed=250，界面显示前进速度 900 km/h（×3.6）——决定飞机平飞的最高速度，也参与转弯半径计算（配合 MaxSpeedYaw）。 |
| `AfterburnSpeed` | float | 加力速度：加力飞行速度。例：MiG-35 加力可达 350（约 1260 km/h），远超平飞极速——开加力可短时大幅提速，用于追击/脱离。 |
| `AfterburnCornerSpeed` | float | 加力角速度点：加力最佳转弯速度。例：MiG-35 加力角速度点 308，加力时在此速度转弯最佳——决定加力状态的最优转弯速度。 |
| `Acceleration` | float | 加速度：加速度。例：MiG-35 加速度 10，加力时 20——决定飞机多快达到目标速度，影响追击和摆脱。 |
| `AfterburnerAcceleration` | float | 加力加速度：加力加速度。例：MiG-35 加力加速度 20，是正常 2 倍——开加力时提速更快。 |
| `Deceleration` | float | 减速度：减速度。例：MiG-35 减速度 15，加力时 25——决定飞机减速多快，影响急停和进入低速机动。 |
| `AfterburnerDeceleration` | float | 加力减速度：加力减速度。例：MiG-35 加力时减速 25——从加力极速降下来更快。 |
| `MinSpeedYaw` | float | 低速偏航：低速偏航速率。例：低速时偏航灵活度——决定低速段的转向能力。 |
| `MaxSpeedYaw` | float | 高速偏航：高速偏航速率（度/秒）。例：MiG-35 的 55 度/秒使转弯半径 = 2×MaxSpeed×(tan(MaxSpeedYaw×π/180)+1)÷tan(MaxSpeedYaw×π/180) = 2×250×(tan(55°)+1)÷tan(55°) ≈ 850 m——决定飞机盘旋的灵活度，数值越大转弯越急。 |
| `AfterburnYaw` | float | 加力偏航：加力偏航速率。例：开加力时的偏航灵活度——决定加力状态的转向能力。 |
| `CornerSpeedYaw` | float | 角速度点偏航：角速度点偏航速率（度/秒，只读：由 MinSpeedYaw/MaxSpeedYaw 按 CornerSpeed 位置插值）。例：MiG-35 为 50.8——注意：界面显示的转弯半径不使用此字段，而是由 MaxSpeedYaw + MaxSpeed 计算（见 MaxSpeedYaw）。 |
| `CornerAfterburnSpeedYaw` | float | 加力角速度点偏航：加力角速度点偏航速率。例：加力最优转弯速度处的偏航速率——决定加力最优转弯的灵活度。 |
| `MinSpeedPitch` | float | 低速俯仰：低速俯仰速率。例：决定飞机低速时的俯仰灵活性，影响爬升/俯冲的响应。 |
| `MaxSpeedPitch` | float | 高速俯仰：高速俯仰速率。例：决定高速飞行时的俯仰能力——影响高速俯冲拉起和规避。 |
| `AfterburnPitch` | float | 加力俯仰：加力俯仰速率。例：开加力时的俯仰灵活度——决定加力状态的爬升/俯冲能力。 |
| `MaxRoll` | float | 最大滚转：最大滚转角（度）。例：MiG-35 可滚转 90°，Tu-160 仅 60°——滚转角越大，飞机能做越剧烈的横滚机动。 |
| `NoiseSpeed` | float | 噪声速度：飞行噪声速度系数。例：噪声模拟飞行的微小抖动速度——决定飞行时的噪声动画。 |
| `NoiseSize` | float | 噪声尺寸：飞行噪声尺寸系数。例：噪声抖动的幅度——决定飞行时抖动的强度。 |
| `LocalShakingDepth` | float | 局部抖动深度：机体局部抖动深度。例：机体局部的抖动幅度——决定机身抖动的视觉效果。 |
| `LocalShakingSpeed` | float | 局部抖动速度：机体局部抖动速度。例：机身抖动的频率——决定抖动动画的快慢。 |
| `NoseLiftStartSpeed` | float | 抬头起始速度：机头抬起起始速度。例：达到此速度时机头开始抬起——决定起飞/爬升时的机头动作。 |
| `NoseLiftAngle` | float | 抬头角：机头抬起角度。例：爬升时机头的仰角——决定机头抬起的高度。 |
| `ForceCounterClockwise` | bool | 强制逆时针：是否强制逆时针盘旋。例：true 的飞机只逆时针盘旋——用于特定飞行路径/战术动作。 |
| `TargetAheadFactor` | float | 目标前置系数：目标前置系数。例：决定飞机瞄准时提前量的计算——影响机炮/火箭对移动目标的命中。 |
| `StrafeDiveAngle` | float | 俯冲扫射角：俯冲扫射角。例：攻击机俯冲扫射的角度——决定对地扫射时的俯冲姿态和命中窗口。 |
| `StrafeSpeedRatio` | float | 扫射速度比：俯冲扫射速度比。例：扫射时的速度比例——决定俯冲扫射的进入速度。 |

## Sensors（传感器）

> 观瞄/雷达传感器模板（地面/低空/高空 视野距离）。 （行数：36）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `Name` | str | 名称：内部英文名（数据库内唯一标识，非显示名）。例：'BTR-82'、'M1A2 SEP v2 TUA'——用于表间引用和代码查找，玩家看到的是 HUDName。 |
| `IsDefault` | bool | 是否默认：是否为该表的默认选项。例：IsDefault=true 的装甲/传感器是单位的初始配置，无需额外选择即可使用——标记基础项。 |
| `OpticsGround` | float | 地面视野：地面视野距离。例：Good Optics 地面视野值 875（游戏内 1750m），能在此距离内发现地面目标——决定对地探测距离，先发现先开火。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `OpticsHighAltitude` | float | 高空视野：高空视野距离。例：Good Optics 高空视野值 1000（游戏内 2000m）——决定对高空目标的探测能力。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `OpticsLowAltitude` | float | 低空视野：低空视野距离。例：Good Optics 低空视野值 1100（游戏内 2200m）——决定对低空目标（直升机/无人机）的探测能力。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `ModelFileName` | str | 模型文件名：3D 模型资源名。例：BTR-82 用 'RU_BTR82A' 模型——修改它可替换单位在游戏中的 3D 外观。 |

## Turrets（炮塔）

> 炮塔/武器站定义（旋转范围、转速、是否全向旋转、父子炮塔）。 （行数：1055）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `Name` | str | 名称：内部英文名（数据库内唯一标识，非显示名）。例：'BTR-82'、'M1A2 SEP v2 TUA'——用于表间引用和代码查找，玩家看到的是 HUDName。 |
| `ModelFileName` | str | 模型文件名：3D 模型资源名。例：BTR-82 用 'RU_BTR82A' 模型——修改它可替换单位在游戏中的 3D 外观。 |
| `IsDefault` | bool | 是否默认：是否为该表的默认选项。例：IsDefault=true 的装甲/传感器是单位的初始配置，无需额外选择即可使用——标记基础项。 |
| `IsDynamicTurret` | bool | 是否动态炮塔：是否动态炮塔。例：动态炮塔（如可升降/伸缩的武器站）与非动态炮塔行为不同——影响炮塔的动画与运动逻辑。 |
| `ParentTurretId` | int | 父炮塔 ID：父炮塔 ID（嵌套炮塔）。例：顶置机枪可能是主炮塔的子炮塔——用于构建多级炮塔挂载结构。 |
| `FullRotation` | bool | 全向旋转：是否可 360° 全向旋转。例：多数炮塔 true，可全向射击；固定武器站 false，只能扫射前方扇区——决定武器能覆盖的角度范围。 |
| `LeftHorizontalAngle` | float | 左旋转角：向左旋转最大角度。例：-180 表示可向左转满 180°——与右角度共同定义水平射界。 |
| `RightHorizontalAngle` | float | 右旋转角：向右旋转最大角度。例：180 表示可向右转满 180°——决定炮塔右侧射界。 |
| `HorizontalRotationSpeed` | float | 水平转速：水平旋转速度（度/秒）。例：M10Booker 炮塔 75 度/秒，转得快能快速跟瞄移动目标——决定炮塔转向的灵活度。 |

## Abilities（能力）

> 单位特殊能力（烟雾、APS 主动防御、雷达、诱饵、补给、潜行、激光指示等）。 （行数：78）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `Name` | str | 名称：内部英文名（数据库内唯一标识，非显示名）。例：'BTR-82'、'M1A2 SEP v2 TUA'——用于表间引用和代码查找，玩家看到的是 HUDName。 |
| `IsDefault` | bool | 是否默认：是否为该表的默认选项。例：IsDefault=true 的装甲/传感器是单位的初始配置，无需额外选择即可使用——标记基础项。 |
| `ModelFileName` | str | 模型文件名：3D 模型资源名。例：BTR-82 用 'RU_BTR82A' 模型——修改它可替换单位在游戏中的 3D 外观。 |
| `ECMAccuracyMultiplier` | float | 电子干扰精度倍率：电子干扰精度倍率。例：ECM 能力会降低敌方命中精度（倍率>0 生效）——电子干扰让来袭导弹更难命中。 |
| `IsRadar` | bool | 是否雷达：是否雷达能力。例：雷达开启后能远距离发现目标，并配合雷达制导武器——雷达能力用于侦察和引导。 |
| `RadarLowAltOpticsModifier` | float | 雷达低空观瞄修正：雷达开启时低空观瞄修正。例：修正>1 增强对低空目标的探测，<1 减弱——决定雷达对低空目标的观瞄加成。 |
| `RadarHighAltOpticsModifier` | float | 雷达高空观瞄修正：雷达开启时高空观瞄修正。例：修正>1 增强对高空目标的探测——决定雷达对高空目标的观瞄加成。 |
| `RadarLowAltWeaponRangeModifier` | float | 雷达低空射程修正：雷达开启时低空武器射程修正。例：修正>1 延长对低空目标的武器射程——决定雷达对低空交战距离的加成。 |
| `RadarHighAltWeaponRangeModifier` | float | 雷达高空射程修正：雷达开启时高空武器射程修正。例：修正>1 延长对高空目标的武器射程——决定雷达对高空交战距离的加成。 |
| `RadarSwitchCooldown` | float | 雷达开关冷却：雷达开关冷却时间。例：雷达开启/关闭后需冷却才能再切换——防止频繁开关雷达。 |
| `IsRadarStatic` | bool | 是否固定雷达：是否为固定（不可移动）雷达。例：固定雷达站为 true，车载雷达为 false——决定雷达是否需要载具平台。 |
| `IsLaserDesignator` | bool | 是否激光指示：是否激光指示器。例：激光指示器照射目标，供激光制导导弹/炸弹命中——激光指示是激光制导武器的前提。 |
| `LaserMaxRange` | float | 激光最大距离：激光指示最大距离。例：激光指示器只能在最大距离内照射目标——决定激光制导武器的有效引导范围。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `LaserUsableInMove` | bool | 移动中可用激光：移动中是否可用激光指示。例：true 的激光指示器可在行进间照射目标——决定能否边走边引导激光制导武器。 |
| `IsInfantrySprint` | bool | 是否步兵冲刺：是否步兵冲刺能力。例：步兵冲刺可短时提高移速——用于快速穿越开阔地或抢占掩体。 |
| `SprintDuration` | float | 冲刺持续时间：冲刺持续时间。例：冲刺持续几秒后恢复常速——决定冲刺能维持多久。 |
| `SprintCooldown` | float | 冲刺冷却：冲刺冷却时间。例：冲刺用完需冷却——决定冲刺的使用频率。 |
| `IsSmoke` | bool | 是否烟雾：是否为烟雾能力。例：烟雾弹能力 IsSmoke=true，释放烟幕遮蔽视线——用来掩护己方单位推进或撤退，阻断敌方瞄准。 |
| `SmokeAmmunitionId` | int | 烟雾弹药 ID：烟雾能力所用弹药（外键 → Ammunitions）。例：指向烟幕弹弹药条目——决定烟雾能力释放哪种烟幕。 |
| `SmokeAmmunitionQuantity` | int | 烟雾弹药数量：烟雾弹数量。例：Smoke x2 有 2 发烟幕，可释放两次——数量决定该能力能用几次。 |
| `SmokeCooldown` | float | 烟雾冷却：烟雾冷却时间。例：烟雾冷却 5 秒，用完后需等待才能再用——决定烟幕能力的释放频率。 |
| `IsAPS` | bool | 是否主动防御：是否主动防御系统（APS）。例：Trophy APS 为 true，可自动拦截来袭火箭弹/导弹——主动防御能击落飞来的反坦克弹药，大幅提升生存力。 |
| `APSHitboxProportion` | float | 主动防御拦截率：APS 拦截命中弹的比例。例：Trophy 为 2.0（可理解为拦截概率/覆盖系数）——值越高，拦截来袭弹药的成功率越高。 |
| `APSQuantity` | int | 主动防御弹药量：APS 拦截次数。例：Trophy 有 4 次拦截，能连续挡下 4 发来袭弹药——次数决定 APS 能防住多少发攻击，耗尽后失效。 |
| `APSCooldown` | float | 主动防御冷却：APS 拦截冷却。例：Trophy 拦截冷却 6 秒，两次拦截之间需等待——决定 APS 的拦截节奏。 |
| `APSSupplyCost` | float | 主动防御补给费：APS 补给消耗。例：Trophy 补给一次 200 点——决定补充 APS 拦截次数需要多少补给。 |
| `APSResupplyTime` | float | 主动防御补给时间：APS 补给时间。例：Trophy 补给需 10 秒——决定补充 APS 拦截次数的耗时。 |
| `IsDecoy` | bool | 是否诱饵：是否为诱饵/干扰能力。例：诱饵弹释放热焰/箔条，诱骗来袭导弹偏离——用于对抗红外/雷达制导武器。 |
| `DecoyFXPreset` | unknown | 诱饵特效预设：诱饵特效预设资源名。例：指向热焰/箔条的特效预设——决定诱饵释放时的视觉效果。 |
| `DecoyQuantity` | int | 诱饵数量：诱饵数量。例：诱饵 4 发，可连续释放 4 次——决定诱饵能使用几次。 |
| `DecoyAccuracyMultiplier` | float | 诱饵精度倍率：诱饵对敌方命中精度的影响倍率。例：倍率<1 降低敌方导弹命中率——决定诱饵的干扰强度。 |
| `DecoyDuration` | float | 诱饵持续时间：诱饵持续时间。例：诱饵持续几秒，期间持续干扰来袭导弹——决定诱饵的有效时间窗口。 |
| `DecoyCooldown` | float | 诱饵冷却：诱饵冷却时间。例：诱饵用完需冷却才能再释放——决定诱饵的使用频率。 |
| `DecoySupplyCost` | float | 诱饵补给费：诱饵补给消耗。例：补充诱饵需消耗补给——决定诱饵的补给成本。 |
| `DecoyResupplyTime` | float | 诱饵补给时间：诱饵补给时间。例：补充诱饵需时间——决定补充诱饵的耗时。 |
| `IsResupplyAbility` | bool | 是否补给能力：是否为补给能力。例：补给单位的能力为 true，可为友军补弹药——标记该能力属于补给类。 |
| `ResupplyIconType` | int | 补给图标类型：补给图标类型枚举。例：不同补给类型（弹药/维修/医疗）用不同图标——决定界面显示的补给图标。 |
| `ResupplyRadius` | float | 补给半径：补给范围半径。例：补给能力在半径内为友军补充——决定补给能覆盖多大区域。 注：游戏内距离 = 此值 ×2（值 200 → 游戏内 400m）。 |
| `CanHeal` | bool | 可治疗：是否可治疗。例：医疗单位 CanHeal=true，能治疗受伤步兵——用于恢复己方步兵生命。 |
| `CanRepair` | bool | 可维修：是否可维修。例：工程单位 CanRepair=true，能修复受损载具——用于恢复己方车辆生命。 |
| `CanResupply` | bool | 可补给：是否可补给弹药。例：补给单位 CanResupply=true，能为友军补充弹药——用于维持前线持续作战。 |
| `ResupplyPoints` | int | 补给点数：补给点数。例：补给单位拥有一定补给点，按弹药消耗——决定能补给多少弹药。 |
| `CanSelfSupply` | bool | 可自我补给：是否可自我补给。例：true 的单位能给自己补弹药——自给自足，无需外部补给点。 |
| `CanWorkOnMove` | bool | 移动中可用：移动中是否可用该能力。例：true 的能力在行进间也能生效——决定能力是否受移动限制。 |
| `SupplyPointCost` | float | 补给点消耗：每点补给的消耗。例：补给 1 点需消耗一定资源——决定补给的性价比。 |
| `SupplyTargetsMask` | int | 补给目标掩码：可补给目标类型位掩码。例：掩码决定能补给哪些目标（步兵/载具/飞机等）——限定补给能力的适用对象。 |
| `IsSneakAbility` | bool | 是否潜行能力：是否潜行能力。例：潜行单位更难被发现，适合渗透——潜行能力提升隐蔽，降低被探测距离。 |
| `SneakCoverBonus` | float | 潜行掩体加成：潜行时掩体加成。例：潜行单位在掩体中隐蔽加成更高——掩体中的潜行单位更难被发现。 |
| `SneakVehicleSpeedMultiplier` | float | 潜行载具速度倍率：潜行时载具速度倍率。例：潜行载具速度×倍率（通常 <1 更慢更隐蔽）——决定潜行对载具速度的影响。 |
| `IsArtilleryAutoFire` | bool | 是否炮火自动射击：是否炮火自动射击。例：炮兵自动射击能力让火炮自动轰击来袭目标——用于自动火力响应。 |

## Modifications（改装）

> 单位的改装槽（装甲/武器/引擎/光学/能力/班组/变体），每个槽含若干选项。 （行数：615）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `UnitId` | int | 单位 ID：单位 ID（外键 → Units）。例：1=BTR-82——把链接行关联到某个单位。 |
| `Name` | str | 名称：内部英文名（数据库内唯一标识，非显示名）。例：'BTR-82'、'M1A2 SEP v2 TUA'——用于表间引用和代码查找，玩家看到的是 HUDName。 |
| `Type` | int | 改装类型：改装槽类型（见枚举表）。例：5=装甲槽（Armor StrykerICV），3=武器包槽（Weapons Stryker ICV），2=引擎槽，4=光学槽——决定该改装槽能挂载哪类升级。 |
| `UIName` | str | 界面名：界面显示名称（本地化键）。例：'Custom_Slot_Armor' 对应界面装甲槽——指向本地化文本。 |
| `ThumbnailFileName` | unknown | 缩略图：缩略图资源名。例：改装槽的缩略图——决定改装界面显示的图标。 |
| `Order` | int | 排序：改装槽在界面中的排序。例：Order 越小越靠前——决定改装槽在单位改装界面里的显示顺序。 |

## Options（选项）

> 改装的具体选项，定义替换的装甲/机动/炮塔/能力及价格、贴图等。 （行数：2045）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `IsDefault` | bool | 是否默认：是否为该改装槽的默认选项。例：默认选项无需额外点数，是单位的初始配置——标记基础形态。 |
| `ModificationId` | int | 改装 ID：所属改装（外键 → Modifications）。例：把选项归属到某个改装槽——决定该选项出现在哪个改装槽下。 |
| `Name` | str | 名称：内部英文名（数据库内唯一标识，非显示名）。例：'BTR-82'、'M1A2 SEP v2 TUA'——用于表间引用和代码查找，玩家看到的是 HUDName。 |
| `Order` | int | 排序：排序序号（数字越小越靠前）。例：Order=0 的改装槽/武器排在最前——决定列表与挂载的显示顺序。 |
| `UIName` | str | 界面名：界面显示名称（本地化键）。例：'Custom_Option_Default' 对应默认选项——指向本地化文本。 |
| `ConcatenateWithUnitName` | str | 拼接单位名：是否将单位名与该后缀拼接显示。例：' Javelin' 拼到单位名后显示为 'Stryker Javelin'——用于显示改装后的完整名称。 |
| `ReplaceUnitName` | str | 替换单位名：替换显示的单位名。例：'M1296 Dragoon' 替换原单位名——改装后显示全新名称。 |
| `ReplaceUnitId` | int | 替换单位 ID：替换成的单位（外键 → Units，0=不替换）。例：武器包选项把 Stryker 替换成 M6Linebacker 变体——选择后单位整体变成另一个单位（换装/变体）。 |
| `Cost` | int | 价格：该选项额外价格（点数）。例：装甲升级选项 Cost=15，选择后卡组点数额外 +15——改装选项越强，额外点数越高。 |
| `TimeModifierRefund` | float | 撤退返还修正：撤退返还时间修正。例：正值让选择该选项的单位撤退时返还更多——影响撤退收益。 |
| `TimeModifierResurrect` | float | 复活时间修正：复活等待时间修正。例：负值缩短复活等待——决定选择该选项后单位复活快慢。 |
| `OptionPicture` | str | 选项图片：选项示意图标资源名。例：'M2_BROWNING' 是重机枪选项的图片——决定改装选项显示的示意图。 |
| `ThumbnailOverride` | str | 缩略图覆盖：覆盖单位缩略图。例：选项换装后覆盖缩略图为新单位——决定改装后列表缩略图。 |
| `PortraitOverride` | str | 头像覆盖：覆盖单位头像路径。例：选项换装后覆盖头像——决定改装后界面头像。 |
| `AudioPresetOverride` | str | 音效覆盖：覆盖单位音效预设。例：选项换装后覆盖音效（如换导弹的音效）——决定改装后的声音。 |
| `ArmorId` | int | 装甲 ID：替换的装甲（外键 → Armors，0=不变）。例：装甲改装选项把 ArmorId 换成更强的装甲套件——选择后单位的防护值改变。 |
| `MobilityId` | int | 机动性 ID：替换的机动性（外键 → Mobility，0=不变）。例：引擎改装选项换成更快/更强的机动模板——选择后单位的速度/机动改变。 |
| `MainSensorId` | int | 主传感器 ID：替换的主传感器（外键 → Sensors）。例：光学改装选项换成视野更远的传感器——选择后单位的探测距离提升。 |
| `ExtraSensorId` | int | 副传感器 ID：替换的副传感器（外键 → Sensors）。例：加装副传感器提升探测——决定改装后的副观瞄/雷达。 |
| `StealthOverride` | float | 隐蔽覆盖：隐蔽覆盖。例：某些选项会覆盖单位的隐蔽值（>0 生效）——用于改装改变隐蔽性。 |
| `IsDynamicTurretOption` | bool | 是否动态炮塔选项：是否为动态炮塔选项。例：动态炮塔（升降武器站）选项为 true——标记该选项改变炮塔动态。 |
| `Ability1Id` | int | 能力1 ID：能力槽1（外键 → Abilities）。例：选项给单位挂载一个能力（如烟雾）——决定改装后单位获得的第 1 个能力。 |
| `Ability2Id` | int | 能力2 ID：能力槽2（外键 → Abilities）。例：第 2 个能力槽——决定改装后单位获得的第 2 个能力。 |
| `Ability3Id` | int | 能力3 ID：能力槽3（外键 → Abilities）。例：第 3 个能力槽——决定改装后单位获得的第 3 个能力。 |
| `Turret0Id` | int | 炮塔0 ID：炮塔槽0（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret1Id` | int | 炮塔1 ID：炮塔槽1（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret2Id` | int | 炮塔2 ID：炮塔槽2（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret3Id` | int | 炮塔3 ID：炮塔槽3（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret4Id` | int | 炮塔4 ID：炮塔槽4（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret5Id` | int | 炮塔5 ID：炮塔槽5（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret6Id` | int | 炮塔6 ID：炮塔槽6（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret7Id` | int | 炮塔7 ID：炮塔槽7（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret8Id` | int | 炮塔8 ID：炮塔槽8（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret9Id` | int | 炮塔9 ID：炮塔槽9（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret10Id` | int | 炮塔10 ID：炮塔槽10（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret11Id` | int | 炮塔11 ID：炮塔槽11（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret12Id` | int | 炮塔12 ID：炮塔槽12（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret13Id` | int | 炮塔13 ID：炮塔槽13（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret14Id` | int | 炮塔14 ID：炮塔槽14（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret15Id` | int | 炮塔15 ID：炮塔槽15（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret16Id` | int | 炮塔16 ID：炮塔槽16（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret17Id` | int | 炮塔17 ID：炮塔槽17（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret18Id` | int | 炮塔18 ID：炮塔槽18（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret19Id` | int | 炮塔19 ID：炮塔槽19（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |
| `Turret20Id` | int | 炮塔20 ID：炮塔槽20（外键 → Turrets）。例：把选项的某个炮塔槽指向具体炮塔，0=空槽——决定改装后该槽挂载哪个炮塔。 |

## Specializations（专精）

> 师级专精（如 USMC、装甲旅、空降兵），定义卡组槽位数量与点数。 （行数：13）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `CountryId` | int | 阵营 ID：阵营 ID（外键 → Countries）。例：1=Russia、2=USA——把单位/专精归属到对应阵营。 |
| `Name` | str | 名称：内部英文名（数据库内唯一标识，非显示名）。例：'BTR-82'、'M1A2 SEP v2 TUA'——用于表间引用和代码查找，玩家看到的是 HUDName。 |
| `UIName` | str | 界面名：界面显示名称（本地化键）。例：'Custom_Slot_Armor' 对应界面的装甲槽——指向本地化文本表的键，翻译后显示。 |
| `UIDescription` | str | 界面描述：界面描述（本地化键）。例：'ui_spec_usmc_description' 是 USMC 的简介——指向本地化描述文本。 |
| `Icon` | str | 图标：专精图标资源名。例：'SPEC_US_Marines' 是陆战队专精图标——决定专精界面显示的图标。 |
| `Illustration` | str | 插图：专精插图资源名。例：'US_SPEC_USMC' 是陆战队专精大图——决定专精选择界面的插图。 |
| `ShowInHangar` | bool | 在机库显示：是否在机库界面显示。例：true 的专精出现在机库选择里——决定玩家能否看到该专精。 |
| `ContentMembership` | int | 内容归属：DLC/内容包归属。例：-1=本体专精，2=DLC2 专精——决定该专精属于哪个内容包。 |
| `ReconSlots` | int | 侦察槽位：侦察类槽位数量。例：USMC 有 3 个侦察槽——决定该专精卡组能带多少侦察单位。 |
| `InfantrySlots` | int | 步兵槽位：步兵类槽位数量。例：USMC 有 5 个步兵槽——决定卡组能带多少步兵单位。 |
| `CombatSlots` | int | 战斗槽位：战斗类槽位数量。例：USMC 有 4 个战斗槽——决定卡组能带多少坦克/战车单位。 |
| `SupportSlots` | int | 支援槽位：支援类槽位数量。例：USMC 有 7 个支援槽——决定卡组能带多少火炮/防空等支援单位。 |
| `LogisticsSlots` | int | 后勤槽位：后勤类槽位数量。例：多数专精为 0（海军/后勤单位用）——决定卡组能带多少后勤单位。 |
| `HelicoptersSlots` | int | 直升机槽位：直升机类槽位数量。例：USMC 有 6 个直升机槽——决定卡组能带多少直升机。 |
| `AirSlots` | int | 空军槽位：空军类槽位数量。例：USMC 有 7 个空军槽——决定卡组能带多少飞机。 |
| `MaxSlots` | int | 最大槽位：最大槽位总数。例：USMC 上限 32——限制卡组总单位数量，防止无限堆叠。 |
| `ReconPoints` | int | 侦察点数：侦察类点数预算。例：USMC 侦察预算 500 点——决定侦察单位可用的总点数。 |
| `InfantryPoints` | int | 步兵点数：步兵类点数预算。例：USMC 步兵预算 1000——决定步兵单位可用的总点数。 |
| `CombatPoints` | int | 战斗点数：战斗类点数预算。例：USMC 战斗预算 1000——决定坦克/战车可用的总点数。 |
| `SupportPoints` | int | 支援点数：支援类点数预算。例：USMC 支援预算 750——决定火炮/防空可用的总点数。 |
| `LogisticsPoints` | int | 后勤点数：后勤类点数预算。例：多数专精为 0——决定后勤单位可用的总点数。 |
| `HelicoptersPoints` | int | 直升机点数：直升机类点数预算。例：USMC 直升机预算 750——决定直升机可用的总点数。 |
| `AirPoints` | int | 空军点数：空军类点数预算。例：USMC 空军预算 1000——决定飞机可用的总点数。 |

## UnitAbilities（单位-能力关联）

> 链接表：单位与能力（多对多）。 （行数：655）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `UnitId` | int | 单位 ID：单位 ID（外键 → Units）。例：1=BTR-82——把链接行关联到某个单位。 |
| `AbilityId` | int | 能力 ID：能力 ID（外键 → Abilities）。例：1=Smoke x1——给单位挂载一个特殊能力。 |

## UnitPropulsions（单位-机动关联）

> 链接表：单位与机动性模板。 （行数：646）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `UnitId` | int | 单位 ID：单位 ID（外键 → Units）。例：1=BTR-82——把链接行关联到某个单位。 |
| `MobilityId` | int | 机动性 ID：机动性 ID（外键 → Mobility）。例：1=Tracks M1A2 Abrams——给单位指定机动模板。 |

## UnitArmors（单位-装甲关联）

> 链接表：单位与装甲套装。 （行数：711）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `UnitId` | int | 单位 ID：单位 ID（外键 → Units）。例：1=BTR-82——把链接行关联到某个单位。 |
| `ArmorId` | int | 装甲 ID：装甲 ID（外键 → Armors）。例：1=Armor BTR80 Base——给单位指定装甲套件。 |

## SensorUnits（单位-传感器关联）

> 链接表：单位与传感器。 （行数：609）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `UnitId` | int | 单位 ID：单位 ID（外键 → Units）。例：1=BTR-82——把链接行关联到某个单位。 |
| `SensorId` | int | 传感器 ID：传感器 ID（外键 → Sensors）。例：1=Good Optics——给单位指定观瞄/雷达。 |

## TurretUnits（单位-炮塔关联）

> 链接表：单位与炮塔（含挂载顺序）。 （行数：1597）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `UnitId` | int | 单位 ID：单位 ID（外键 → Units）。例：1=BTR-82——把链接行关联到某个单位。 |
| `TurretId` | int | 炮塔 ID：炮塔 ID（外键 → Turrets）。例：2=MainTurret BTR82A——把链接行关联到某个炮塔。 |
| `Order` | int | 排序：排序序号（数字越小越靠前）。例：Order=0 的改装槽/武器排在最前——决定列表与挂载的显示顺序。 |

## TurretWeapons（炮塔-武器关联）

> 链接表：炮塔与武器（含武器通道与优先级、顺序）。 （行数：1280）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `WeaponId` | int | 武器 ID：武器 ID（外键 → Weapons）。例：1=Rifle M27——把链接行关联到某件武器。 |
| `TurretId` | int | 炮塔 ID：炮塔 ID（外键 → Turrets）。例：2=MainTurret BTR82A——把链接行关联到某个炮塔。 |
| `Order` | int | 排序：排序序号（数字越小越靠前）。例：Order=0 的改装槽/武器排在最前——决定列表与挂载的显示顺序。 |
| `WeaponChannel` | int | 武器通道：武器通道编号。例：同一炮塔的多个武器分通道射击——用于分组管理炮塔武器的射击。 |
| `WeaponPriority` | int | 武器优先级：武器优先级。例：优先值高的武器先开火——决定炮塔多武器时的射击顺序。 |

## WeaponAmmunitions（武器-弹药关联）

> 链接表：武器、弹药与携带数量（含单位级覆盖）。 （行数：2358）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `UnitId` | int | 单位 ID：单位 ID（外键 → Units）。例：1=BTR-82——把链接行关联到某个单位。 |
| `WeaponId` | int | 武器 ID：武器 ID（外键 → Weapons）。例：1=Rifle M27——把链接行关联到某件武器。 |
| `AmmunitionId` | int | 弹药 ID：弹药 ID（外键 → Ammunitions）。例：1=Round 120 M256 AP——把武器关联到具体弹药。 |
| `Order` | int | 排序：排序序号（数字越小越靠前）。例：Order=0 的改装槽/武器排在最前——决定列表与挂载的显示顺序。 |
| `Quantity` | int | 数量：携带的该弹药数量。例：坦克带 40 发穿甲弹、8 发破甲弹——决定该武器某种弹药的携带量。 |

## SquadMembers（班组成员）

> 步兵班组成员定义（模型、主武器、特殊武器、死亡优先级）。 （行数：1134）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `DeathPriority` | int | 死亡优先级：班组成员死亡替换优先级。例：优先级决定班组成员阵亡时的替换顺序——影响班组伤亡时的补位逻辑。 |
| `ModelFileName` | str | 模型文件名：3D 模型资源名。例：BTR-82 用 'RU_BTR82A' 模型——修改它可替换单位在游戏中的 3D 外观。 |
| `PrimaryWeaponId` | int | 主武器 ID：主武器（外键 → Weapons）。例：步枪手 PrimaryWeaponId 指向步枪——决定班组成员的主武器。 |
| `SpecialWeaponId` | int | 特殊武器 ID：特殊武器（外键 → Weapons，0=无）。例：火箭手 SpecialWeaponId 指向火箭筒——决定班组成员携带的特殊武器。 |
| `UnitId` | int | 单位 ID：单位 ID（外键 → Units）。例：1=BTR-82——把链接行关联到某个单位。 |

## SquadWeapons（班组武器）

> 链接表：班组携带的武器（含顺序）。 （行数：706）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `WeaponId` | int | 武器 ID：武器 ID（外键 → Weapons）。例：1=Rifle M27——把链接行关联到某件武器。 |
| `Order` | int | 排序：排序序号（数字越小越靠前）。例：Order=0 的改装槽/武器排在最前——决定列表与挂载的显示顺序。 |
| `UnitId` | int | 单位 ID：单位 ID（外键 → Units）。例：1=BTR-82——把链接行关联到某个单位。 |

## SpecializationAvailabilities（专精-单位可用性）

> 链接表：专精与单位的可用数量（按经验等级 XP0~XP3）。 （行数：338）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `SpecializationId` | int | 专精 ID：专精 ID（外键 → Specializations）。例：3=USMC——把可用性/运输归属到某个专精。 |
| `UnitId` | int | 单位 ID：单位 ID（外键 → Units）。例：1=BTR-82——把链接行关联到某个单位。 |
| `MaxAvailabilityXp0` | int | XP0 可用数：经验等级 0 时最大可用数量。例：刚解锁时最多带 4 辆该单位——决定单位在初始经验等级的可部署上限。 |
| `MaxAvailabilityXp1` | int | XP1 可用数：经验等级 1 时最大可用数量。例：升级后可带更多（0=不增加）——决定单位随等级提升的可部署上限。 |
| `MaxAvailabilityXp2` | int | XP2 可用数：经验等级 2 时最大可用数量。例：进一步解锁更多——决定更高级别的可部署上限。 |
| `MaxAvailabilityXp3` | int | XP3 可用数：经验等级 3 时最大可用数量。例：满级最多可部署的数量——决定单位的最终可部署上限。 |

## TransportAvailabilities（运输可用性）

> 链接表：可运输的单位（挂在专精可用性之下）。 （行数：227）

| 字段 | 类型 | 含义 |
|---|---|---|
| `Id` | int | 编号：唯一 ID，其他表通过外键引用。例：单位 1=BTR-82，武器/弹药表通过 UnitId/WeaponId 指向它——数据库主键，修改会破坏所有引用，请谨慎。 |
| `SpecializationAvailabilityId` | int | 专精可用性 ID：所属专精可用性（外键 → SpecializationAvailabilities）。例：把可运输单位挂到某个专精可用性下——决定该运输项属于哪个专精。 |
| `UnitId` | int | 单位 ID：单位 ID（外键 → Units）。例：1=BTR-82——把链接行关联到某个单位。 |

## 数据结构与文件格式

### 数据库加密（DataBaseCompiled）

- 资产：`data.unity3d` 内 `DataBaseCompiled`（UABEA 路径 51978），24 张表 JSON 字段。
- 每个表字段 = `base64("fhk3s0g3"(8B 魔数) + IV(16B 随机) + AES-256-CBC(明文JSON, PKCS#7))`。
- 密钥 = ASCII `09234237536700238099172758697347`（32 字节）；每次加密用新随机 IV。
- 明文 = 紧凑 JSON 数组（Unity JsonUtility 风格）。
- 加载路径：游戏按 `Resources/DataBaseCompiled` 从 data.unity3d 加载
  （类 BrokenArrow.Shared.Ecs.DataBaseCompiled，`DeserializeCrypt<T>` 为其解密入口）。

### AssetBundle（UnityFS）

- 容器：magic `UnityFS` + u32 格式版本(8) + 版本串 + u64 总大小 + 块信息大小×2 + flags（大端）。
- flags：0x3F=压缩类型（0 无/1 lzma/2 lz4/3 lz4hc），0x40=块信息含目录，0x80=块信息在文件尾，
  0x200=块信息前对齐。游戏实测：77 个 bundle 用 0x243（LZ4HC），units bundle 独用
  0xC2（LZ4 + 块信息在文件尾，41,695 块）。
- 数据按 128KB 分块；compSize==uncompSize 表示该块未压缩。
- 序列化文件 v22：对象目录（metadata）在文件开头——只解压首块即可枚举对象。
- 对象用 pathID(int64) 标识；PPtr = {m_FileID:i32, m_PathID:i64}。

### CRC（已破解，全量验证）

- **bundle CRC = zlib.crc32(所有数据块解压后的字节按序拼接)**，不是对文件字节算。
- catalog.json 的 AssetBundleRequestOptions 里 `m_Crc` 存十进制。
- 改任何 bundle 内容后必须重算并写回 catalog：
  `python compute_bundle_crc.py --update-catalog <aa\catalog.json>`。
- 实测：units bundle CRC=3612175247，78 个 bundle 中 77 个与 catalog 100% 一致。
- 游戏校验失败时 GameLogs 报
  `CRC Mismatch. Provided <hex>, calculated <hex> from data. Will not load AssetBundle '<名>'`
  —— 日志里的 calculated 值可直接写回 catalog。

### catalog.json（Addressables 1.19+ 新格式）

- 4 个 base64 二进制字段：m_KeyDataString / m_BucketDataString / m_EntryDataString /
  m_ExtraDataString；字节标签对象：0=ASCII 1=Unicode(UTF-16LE) 2=UInt16 3=UInt32 4=Int32
  5=Hash128 6=Type 7=JsonObject(程序集名+类名+UTF-16LE JSON)。
- entry = 7×int32：0=InternalId 索引、1=Provider 索引、2=DependencyKey（依赖集键索引）、
  3=DepHash、4=ExtraData 对象偏移、5=PrimaryKey、6=ResourceType。
- 资产条目（BundledAssetProvider）的 InternalId 是**资产路径**；所在 bundle 经 DependencyKey
  解析——依赖集键（字符串负哈希，如 "-1968431308"）的 bucket 第一个条目 = 所在 bundle。
- 文件名锚点 = AssetBundleRequestOptions.m_Hash（文件名 = `<名字>_<m_Hash>.bundle`）。
- 规模：28,938 键、21,275 条目、14,455 InternalIds、77 个 bundle。

### .bamod 素材包

- zip（manifest.json）：`{format:"bamod-assets", version:1, bundle, prefab_path, root_pid,
  preload:[MonoScript 常量 pid + 对象 pid], objects:[{pid, class_id, script_id, tree_hash,
  raw(b64)}]}`；纹理模式另含 `textures:[{path, png(b64)}]`；皮肤模式含 `skin:{...}`
  （见「载具皮肤」节；皮肤包不清理容器条目）。
- 导入：清理旧 mod 对象（pid 段 0x4355424500000000~+0x10000）+ 旧容器/preload 条目
  （含 preloadIndex 重映射）→ 类型匹配（script_id→tree_hash→class_id）→ 新 pid 单遍字节重映射
  → 补容器条目 + preload（MonoScript 前置！）→ lz4 保存 → 重算 CRC → 可选注册地址。

### 音频（FMOD 音库）

- 游戏音频全部在 `BrokenArrow_Data/StreamingAssets` 的 FMOD Studio 音库
  （`*.bank`：RIFF-FMT-LIST-SND 结构，新版 FMOD，无 FSB5 标记；流式音频 `*.gts/*.gtp`）；
  bundle 内无 AudioClip，不走 .bamod 管线。
- StreamingAssets 无 CRC 校验 → 音库整文件替换即可生效；BA_Mod_Maker
  文件 →「导入音频/音效」做列表/备份/替换/还原/批量（备份在 `_ba_audio_backup/`）。
- 样本级编辑（提取/重混单个音效）需解析 FMOD Studio 银行元数据，属独立逆向项目。

### IL2CPP 组件（prefab 侧）

- 开发方 Steel Balalaika Studio；主程序集 BrokenArrow.dll（BrokenArrow / BrokenArrow.Client.Ecs /
  Shared.Ecs / DataBase.Models / MissionEditor / ScriptEngine 命名空间）。
- MonoBehaviour 序列化 = m_GameObject PPtr(12B) + m_Enabled(1B)+pad(3B) + m_Script PPtr(12B)
  + m_Name 字符串 + 派生字段；m_Script pathID 在字节偏移 20。
- 模型类：BrokenArrow.DataBase.Models.{Units,Weapons,Ammunitions,Turrets,Abilities,...}
  （SQLite 特性，24 表镜像）。

### 网格顶点流

- 通道索引语义（固定）：0=位置 1=法线 2=切线 4=UV0 5=UV1 12=蒙皮权重 13=骨骼索引。
- format：0=float32 1=float16 10=uint32 11=int32；内联数据每流按 16 字节对齐，
  流式(.resS)数据紧密排列。
- 流式网格：Mesh.m_StreamData = {path:"archive:/CAB-<hash>/CAB-<hash>.resS", offset, size}，
  实际文件在 `<bundle>_unpacked/CAB-<hash>.resS`。
- **骨骼名哈希（已破解 v1.8.0）**：`zlib.crc32(从 "root" 起的层级路径, '/' 连接, UTF-8)`；
  跳过 root_scale 节点；多索引槽位名取首个 _N（weapon_0_1_2→weapon_0，turret_0_0→turret_0，
  非纯数字尾缀不截断如 frontwing3_3_L）。例：crc32("root/body")=0x65A7524A、
  crc32("root/body/turret_0")=0x2A839EFE。工具用 bone_hashes.bone_hash() 直接计算，
  任意新骨骼名均可（不再依赖查找表）。
- 坐标系：Unity Y-up → Blender Z-up：位置 (x,y,z)→(x,-z,y)；四元数 (x,y,z,w)→(w,x,-z,y)。

### AnimationHub 行为（已破解 v1.8.0）

- MathConnect 布局：lod(i32) + ObjectFollower{freq,damper,reaction,defaultSerialized}(16B)
  + root PPtr + target PPtr + freeze 3 bool(4B) + **WeaponShotForces 字典**。
- **WeaponShotForces 字典布局**（实测样本约束求解）：
  `{i32 1, i32 0, i32 N, N×{i32 0, i64 TransformPathID}, i32 N, N×float}`。
- 全部 22 个行为类字段表已提取（AxisRandom/AxisRepeater/SpawnVFX/ShowVFX/AnimatorConnect/
  TurretFly/ShellCasingDrop 等，含嵌套类型），见 技术资料/extracts/behavior_layouts.json；
  工具侧未完全破译的类仍按字节透传（rawB64），往返无损。

### 载具皮肤（SkinStorageBridge，已破解 v1.8.2）

- 皮肤 = 材质替换：`SkinStorageBridge`（prefab 上的组件）存 `List<SkinStorageData>`，
  每条 = {Id + SkinDataDictionary{材质 PPtr → Renderer[]}}；游戏 SetSkin 把材质赋到对应渲染器。
- 桥字节布局（实测 322 个桥实例，往返无损）：MonoBehaviour 头 32B（含空 m_Name）后
  `i32 数量`；每条 `i32 Id` + `i32 keyCount` + keyCount×PPtr(材质 12B) + `i32 valCount` +
  valCount×{`i32 rCount` + rCount×PPtr(渲染器 12B)}。
- 桥都在炮塔/步兵 prefab 上（车体 prefab 无桥）；坦克炮塔 prefab 自带车体网格
  （如 RU_T90A 含 001_skinned_Chassis）；皮肤材质常跨 prefab 引用（同 bundle 内 pid 直连）。
- 皮肤材质为 HDRP：纹理槽名 `Layer_<hash>`（如 Layer_A97CDC25）；BaseMap 贴图名如
  `T90_1_BaseMap`。贴图多为流式（.resS），提取 PNG 需 `<bundle>_unpacked` 目录。
- 菜单皮肤列表在 data.unity3d 的 UserItemsConfig（SkinGroupDict/SkinPackDict；实例 pid 52173，
  220KB，无 typetree）——**重涂现有皮肤槽无需改它**；新增皮肤槽要在菜单显示需改该资产。
- 工具流：Blender ⑥ 皮肤（扫描/应用/导出贴图/打包 .bamod）→ BA_Mod_Maker 导入
  （新建 RGBA32 纹理 + 克隆材质换纹理槽 + 按皮肤 id 更新全部桥 + CRC）。
  .bamod manifest 新增 `skin:{target_id, new_id, mats:[{orig, texs:[{slot, tex}]}], textures:[{name, png}]}`。

### 步兵模型与姿势（已破解 v1.8.3）

- 步兵单位装配：Units.ModelFileName = 单位锚点（如 US_Rangers.prefab，仅 weapon_0_1/recoil/
  shell_spawn 等挂点，无网格）；SquadMembers.ModelFileName = **成员 = 完整士兵 prefab**。
- 士兵成员 prefab（如 Infantry/Motostrelki/RU_Motostrelki_rifle.prefab）preload 约 137 对象：
  48 GO/Transform（Hips→Spine→Spine1→Spine2→Neck→Head、左右肩/臂/前臂/手/五指、大腿/小腿/脚/脚趾 +
  WeaponLook/WeaponPlace/Heavy Weapons + lod_0/1/2）+ 3 蒙皮网格 + 3 纹理 + 1 材质 +
  Avatar + Animator + AnimatorController + **22 AnimationClip** + LODGroup + SkinStorageBridge。
- **姿势 = 烘焙在该 prefab 的骨骼本地变换里**（如持枪手 RightHand 的局部旋转）；改姿势 =
  改骨骼 Transform 局部旋转/位置，网格与蒙皮不变（bind pose 不变）。容器根 = prefab 同名 GO。
- 角色变体 = 同名系列（_rifle/_MG/_officer/_grenade/_Weapon/_heavy/_rifle1/_rifle_light），
  左右手姿势不同（RU 系左手持枪 + WeaponLook，US 系右手 + WeaponPlace）。
- 工具流：Blender ⑦ 步兵姿势（骨骼列表 + 欧拉滑条编辑 / 姿势库保存应用 / 从其他游戏模型
  按骨骼名复制姿势 / 重置到导入基线）→ copy-full 打包（pose-only，网格字节原样）→
  BA_Mod_Maker 导入；新模型路径留空=原地替换当前姿势，填新路径=新增姿势模型
  （需在数据库把 SquadMembers.ModelFileName 改成新模型名 + 注册地址）。


## 枚举值对照

### Ammunitions.ArmorTargeted

| 值 | 含义 |
|---|---|
| `0` | 无 |
| `1` | 动能穿甲(Kinetic) |
| `2` | 破甲/高爆(HEAT) |

### Ammunitions.Seeker

| 值 | 含义 |
|---|---|
| `0` | 无制导 |
| `10` | 发射后不管 |
| `50` | 末端制导 |
| `100` | 半主动 |
| `200` | 反辐射(SEAD) |
| `300` | 激光制导 |

### Ammunitions.TargetType

| 值 | 含义 |
|---|---|
| `0` | 无 |
| `1` | 地面 |
| `2` | 步兵 |
| `4` | 载具 |
| `8` | 直升机 |
| `16` | 飞机 |
| `32` | 舰船 |
| `128` | 弹丸 |
| `256` | 反辐射导弹 |
| `512` | 巡航导弹 |
| `1024` | 弹道导弹 |
| `其他` | 组合位掩码，按位拆解：1地面/2步兵/4载具/8直升机/16飞机/32舰船/128弹丸 |

### Ammunitions.TrajectoryType

> 弹道类型，同时被「主弹药」判定使用（AmmunitionBoxComponent.IsMainAmmo）：炮兵 Role(130-133) 的主弹药 = 20/30/40/200/300；反坦克步兵 Role(35) 的主弹药 = 100/110/120。

| 值 | 含义 |
|---|---|
| `0` | 无 |
| `10` | 直射 |
| `20` | 曲射(榴弹炮) |
| `30` | 迫击炮弹 |
| `40` | 火箭弹(MLRS) |
| `100` | 追尾导弹 |
| `110` | 前置追尾导弹 |
| `120` | 前置导弹 |
| `200` | 巡航导弹 |
| `300` | 弹道导弹 |
| `400` | 低阻炸弹 |
| `410` | 高阻炸弹 |
| `600` | 激光导弹 |
| `610` | 激光制导自由落体 |
| `666` | 失效弹道(DeadMissile) |

### Ammunitions.UiOptions

| 值 | 含义 |
|---|---|
| `0` | 无 |
| `1` | 轻武器弹药(IsSmallArmsAmmo，HUD 倍率用) |

### Modifications.Type

| 值 | 含义 |
|---|---|
| `0` | 无 |
| `1` | 通用 |
| `2` | 机动 |
| `3` | 炮塔 |
| `4` | 传感器 |
| `5` | 装甲 |
| `6` | 能力 |
| `7` | 整单位替换 |

### Sensors.Type

| 值 | 含义 |
|---|---|
| `5` | 目视 |
| `10` | 红外 |
| `20` | 电视(TV) |
| `30` | 夜视 |
| `100` | 雷达A |
| `110` | 雷达G |
| `120` | 雷达U |
| `300` | 激光 |
| `310` | 波束 |

### Units.CategoryType

> 卡组槽位类别（0 侦察…6 飞机），卡组编辑器的槽位数量按它索引。同一 Role 的单位可以分布在多个槽位类别里，所以改 Role 不会改变卡组槽位。

| 值 | 含义 |
|---|---|
| `-1` | 无 |
| `0` | 侦察 |
| `1` | 步兵 |
| `2` | 载具 |
| `3` | 支援 |
| `4` | 后勤/舰船 |
| `5` | 直升机 |
| `6` | 空军 |

### Units.ContentMembership

| 值 | 含义 |
|---|---|
| `-1` | 无(本体) |
| `2` | DLC2 |

### Units.Role

> 真实作用（反编译实证）：① 出生时决定「哪把武器是主武器」——ATGM 步兵(35)只认 ATGM/ATGMHE，炮兵(130-133)只认榴弹炮/迫击炮/火箭炮与巡航/弹道导弹，防空(15/16/34)只认 MANPAD/SAM/高射炮，其它 Role 全部武器都算主武器；主武器弹药打空 → 语音 + 单位标签警示。② AI 火力任务：AI 火炮用发射单位的 Role 查 AiConfig.TargetingPresetOverrides（冷却/最小目标群造价/首选任务时长/目标 Role 黑名单），未命中回落默认预设；步兵/载具出生时把 Role+Cost 写进 AiTargetComponent。③ 战报与勋章：按 Role 统计击杀 → 7 枚勋章（坦克/战车/步兵/火炮/直升机/防空/飞机）；任务 Lua 可读 LuaUnit.UnitRole。不影响卡组槽位（CategoryType）与可打击目标（武器/弹药）。填枚举外的值不崩溃，走兜底分支。

| 值 | 含义 | 枚举成员 (dump.cs) |
|---|---|---|
| `0` | 无（乘员/飞行员/靶标/舰船等杂项） | `None` |
| `10` | 步兵战车(IFV) | `IFV` |
| `11` | 主战坦克 | `Tank` |
| `12` | 装甲运兵车(APC) | `APC` |
| `13` | 轻型侦察车(LSV) | `LSV` |
| `14` | 卡车/补给 | `CargoTruck` |
| `15` | 远程防空(LRSAM) | `LRSAM` |
| `16` | 近程防空(SRSAM) | `SRSAM` |
| `30` | 一线步兵 | `LineInfantry` |
| `31` | 突击步兵 | `AssaultInfantry` |
| `32` | 侦察步兵 | `ReconInfantry` |
| `33` | 狙击手 | `Snipers` |
| `34` | 防空步兵（便携防空） | `AAInfantry` |
| `35` | 反坦克导弹步兵 | `ATGMInfantry` |
| `36` | 特种部队 | `SpecialForces` |
| `70` | 侦察直升机 | `ReconHelicopter` |
| `71` | 多用途直升机 | `MultiRoleHelicopter` |
| `72` | 重型运输直升机 | `HeavyTransportHelicopter` |
| `73` | 攻击直升机 | `AttackHelicopter` |
| `100` | 无人机 | `Drone` |
| `130` | 火箭炮(MLRS) | `MLRS` |
| `131` | 迫击炮 | `Mortar` |
| `132` | 战术导弹(LAM) | `LAM` |
| `133` | 自行火炮 | `Artillery` |
| `160` | 攻击机 | `AssaultPlane` |
| `161` | 轰炸机 | `Bomber` |
| `162` | 战略轰炸机 | `StrategicBomber` |
| `163` | 运输机 | `TransportPlane` |
| `164` | 多用途战机 | `MultiRolePlane` |
| `200` | 舰船（枚举里有，数据库无单位使用） | `Ship` |
| `201` | 气垫登陆艇(LCAC) | `Hovercraft` |

### Units.Type

> 引擎大类位掩码（2 步兵 / 4 载具 / 8 直升机 / 16 飞机 / 32 舰船 / 128 弹丸 / 256 反辐射 / 512 巡航 / 1024 弹道）。与卡组槽位（CategoryType）、战术角色（Role）是三件不同的事。

| 值 | 含义 |
|---|---|
| `0` | 无 |
| `2` | 步兵 |
| `4` | 载具 |
| `8` | 直升机 |
| `16` | 飞机 |
| `32` | 舰船 |
| `128` | 弹丸 |
| `256` | 反辐射导弹(SEAD) |
| `512` | 巡航导弹 |
| `1024` | 弹道导弹 |

### Weapons.Type

| 值 | 含义 |
|---|---|
| `0` | 未指定 |
| `1` | 步枪 |
| `2` | 精确射手步枪 |
| `3` | 狙击步枪 |
| `4` | 反器材步枪 |
| `5` | 突击步枪 |
| `6` | 战斗步枪 |
| `25` | 冲锋枪(SMG) |
| `26` | 轻机枪 |
| `27` | 中型机枪 |
| `28` | 重机枪(HMG) |
| `29` | 转管机枪 |
| `30` | 班用机枪 |
| `40` | 霰弹枪 |
| `60` | 枪挂榴弹发射器 |
| `61` | 自动榴弹发射器 |
| `62` | 火箭筒(可装填) |
| `63` | 一次性火箭筒 |
| `64` | 手雷 |
| `80` | 反坦克导弹(ATGM) |
| `81` | 高爆反坦克导弹 |
| `82` | 巡航导弹 |
| `83` | 弹道导弹 |
| `84` | 反辐射导弹 |
| `85` | 便携防空(MANPAD) |
| `86` | 防空导弹(SAM) |
| `87` | 中距空空弹(雷达) |
| `88` | 近距空空弹(红外) |
| `89` | 空对地导弹(AGM) |
| `110` | 主炮 |
| `111` | 榴弹炮 |
| `112` | 迫击炮 |
| `113` | 火箭炮(MLRS) |
| `114` | 机关炮 |
| `115` | 高射炮 |
| `116` | 航炮 |
| `140` | 火箭巢 |
| `141` | 外挂机炮 |
| `160` | 普通炸弹 |
| `161` | 减速炸弹 |
| `162` | 制导炸弹 |

## 地址映射与内部路径

### 模型（数据表 ModelFileName 填的地址 ↔ bundle 内部路径，一一对应）

| 类型 | 地址（数据表填） | 内部路径（bundle 内） |
|---|---|---|
| 车体/载具 | `RU_BMPT` | `Assets/Resources_moved/ModelPrefabs/RU/RU_BMPT.prefab` |
| 炮塔 | `RU_BMPT/RU_BMPT1` | `Assets/Resources_moved/ModelPrefabs/RU/Turrets/RU_BMPT/RU_BMPT1.prefab` |
| 新炮塔(例) | `RU_BMPT/RU_BMPT2_MOD` | `Assets/Resources_moved/ModelPrefabs/RU/Turrets/RU_BMPT/RU_BMPT2_MOD.prefab` |
| 飞机 | `RU_AN72P` | `Assets/Resources_moved/ModelPrefabs/Aircraft/RU_AN72P.prefab` |
| 直升机 | `RU_KA52` | `Assets/Resources_moved/ModelPrefabs/Helicopters/RU_KA52.prefab` |
| 机载武器/导弹 | `AIM9_double` | `Assets/Resources_moved/ModelPrefabs/Weapon/AIM9/AIM9_double.prefab` |
| 步兵武器 | `A545` | `Assets/Resources_moved/ModelPrefabs/Infantry Weapons/RU/A545.prefab` |
| 弹药模型 | `100mm_RUS` | `Assets/Resources_moved/Shared/Prefabs/Ammo/100mm_RUS.prefab` |

### 图标（地址 ↔ 内部路径）

| 类型 | 地址（数据表填） | 内部路径（bundle 内） |
|---|---|---|
| 单位肖像 | `RU\BMPT_TERMINATOR\BMPT_TERMINATOR` | `Assets/Resources_moved/Images/UnitPortraits/RU/BMPT_TERMINATOR/BMPT_TERMINATOR.png` |
| 缩略图/标签 | `RU_BMPT-Label` | `Assets/Resources_moved/Images/Labels/Icons/RU_BMPT-Label.png` |
| 武器图标 | `2A72` | `Assets/Resources_moved/Images/Weapons/Icons/2A72.png` |
| 弹药图标 | `AMMO_US_TANK_120_AP` | `Assets/Resources_moved/Images/Ammunition/Icons/AMMO_US_TANK_120_AP.png` |

**命名规律**：
- 新模型的内部路径 = 原模型内部路径，换名加后缀（如 _MOD），保持唯一；
  数据表地址与内部路径同名（如 RU_BMPT → .../RU_BMPT.prefab；炮塔为 目录名/文件名 两段）。
- 前缀 RU = 俄罗斯 / US = 美国；DLC 单位在 DLC3/ 等子目录，照抄原路径所在目录。
- 改模型三步：① bundle 加 prefab（.bamod 导入）② catalog 注册地址
  ③ 数据表 ModelFileName/PortraitFileName/ThumbnailFileName 指向新地址。


## 常量速查

| 常量 | 值 | 用途 |
|---|---|---|
| 数据库魔数 | `fhk3s0g3 (8B)` | 加密字段前缀 |
| 数据库 AES 密钥 | `ASCII 09234237536700238099172758697347` | AES-256-CBC |
| mod 对象 pid 段(units) | `0x4355424500000000("CUBE") ~ +0x10000` | 工具新增对象专属，重导先清理 |
| mod 对象 pid 段(肖像) | `0x4355424600000000("CUBF")` | unitportraits bundle 新增对象 |
| UnitPrefabTurretInfo script pid | `6426374804064612000` | MonoBehaviour m_Script 指向 |
| AnimationHub script pid | `4665939560152279323` | 军械库演示动画 |
| AnimationManagerBridge script pid | `8775279424834731323` | 后坐/开火联动 |
| FmodTurretsTurn script pid | `-1118201132209727813` | 炮塔转向音效 |
| units bundle | `aa/PC/units_assets_all_3cc1eb58d8b7f6cdf82bbfbf3b5b8aaf.bundle (3.66GB)` | 模型主存放区 |
| 炮塔骨骼高度 | `body y=0.863；turret_0 在 body 上方 0.811（世界 y≈1.674）` | 炮塔几何必须在 y≈1.674 |
| 游戏主命名空间 | `BrokenArrow（Steel Balalaika Studio）` | IL2CPP 类所在 |

## 挂载点与组件词典

（数据源 blender_addon/mount_dict.py，全游戏 690 名扫描归纳（历史统计）。）

### 挂载点分类

| 分类 | 说明 |
|---|---|
| 炮塔/旋转座 | 可旋转的炮塔座圈骨骼。turret_N 由 UnitPrefabTurretInfo 驱动旋转（战场 ECS），军械库里由 AnimationHub 演示行为扫掠。 |
| 武器挂点 | 武器俯仰挂点。weapon_0_1_2 = 炮塔0第1武器第2身管；weapon_0_1 = 炮塔0第1武器；weapon_0 = 单武器。TI 里每个 WeaponPointData 对 = weapon(俯仰) + shell_spawn(抛壳)。 |
| 弹药/抛壳点 | 开火抛壳/出膛位置。shell_spawn_X_Y = 第X武器第Y抛壳口；带 (N) 后缀 = 多管/弹舱逐个口。AnimationManagerBridge 的后坐字典键必须指向真实骨骼。 |
| 后坐骨 | 开火后坐动画骨骼（recoil_0_0 = 武器0后坐骨0）。由 AnimationManagerBridge/AnimationManager 驱动。 |
| 观瞄/传感器 | 观瞄镜、瞄准具、雷达、天线。scope/scope_0/scope_01、SpecialScope、radar、antenna 等；天线类常被 AnimationHub 的 AxisRandom/MathConnect 引用做演示晃动。 |
| 特效点 | VFX 位置：开火口焰 fire_y/fire_z、炮塔飞离 turret_fly_VFX、烟幕 VFXPoint_OnSmokeAbility、干扰弹 VFXPoint_OnCMAbility、加力 Afterburner、废烟 exhaust、扬尘 dust_point_*、死亡 death、通用 VFX_point/VFX_N。 |
| 辅助/约束 | helper/aim/Target/anim_*：瞄准辅助、IK 目标、约束参照、动画容器（anim_turret/anim_turret_0），通常带 PositionConstraint/RotationConstraint/LookAtConstraint。 |
| 舱门/座位 | 载员舱门与座位：Entrances/Door/Door (N)、Seats/Seat (N)，供进出舱逻辑挂载。 |
| 轮系/履带 | 履带/负重轮骨骼：Track_L/Track_R、LR000-LR010、RR000-RR010、L001-L006/R001-R006，由悬挂/行走动画系统驱动。 |
| 飞行/旋翼 | 飞行器部件：旋翼 Rotorangle_0/N、机翼 wing_*、发动机 Engine_*、喷口、关节 joint_*。 |
| 结构/骨骼 | 基础结构：root（prefab 根）、body（根骨骼，蒙皮根的父级）、BMPT1 式内部根。 |

高频名称：shell_spawn_0(1083)、weapon_0(964)、VFX_point(739)、root(676)、body(672)、turret_0(494)、death(198)、turret_1(174)、dust_point_left/right(169)、exhaust(148)、shell_spawn_0_0(131)、VFXPoint_OnSmokeAbility(127)、recoil_0_0(96)、weapon_0_1(91)（全游戏扫描频次）。

### 组件（18 条）

| 组件 | 类别 | 关键字段 |
|---|---|---|
| AnimationManager | helper | _root/_body/_wheels/_suspension/_traks(车体动画) · _recoils(dict 武器→recoil_N 后坐挂点) · _weaponsVfxContainer(dict 武器→炮口特效) |
| ContainerSeatInitializer | door | UnitSeatAnimation → 座位动画 |
| DecalProjector | vfx | — |
| DestroyInBattleMarker | helper | — |
| DriverMarker | helper | — |
| EffectsSpawnPoint | vfx | VFXPrefabRef(PPtr 特效预制体) · LifeTime(float) · AttachVfxToObject(bool) · PlayForce(bool) · SoundEventString(str 音效事件名) |
| FastIKFabric | helper | — |
| FmodTurretsTurn | turret | EventName / TargetBone |
| InfantryHandWeapon | weapon | ShellSpawnPosition → 枪口 |
| MaterialQualityManager | helper | — |
| PhysFly | fly | — |
| PlaneInitializer | fly | PilotID → 飞行员 |
| SkinStorageBridge | helper | — |
| SoldierAnimationManager | helper | — |
| UnitPersistentObjectMarker | helper | — |
| UnitPrefabRoot | structure | PrefabRoot / PrefabBody / SpawnTimeObjects / Renderers / Skins.Data |
| UnitPrefabTurretInfo | turret | TurretIndex(int32 炮塔下标，0=主炮塔) · Weapons(list 每项24B：{WeaponTransform(PPtr)→weapon_N 俯仰点, ShellSpawn(PPtr)→shell_spawn_N 开火点}) |
| WeaponPrefabInfo | weapon | ShellSpawns(list 每项：{ShellSpawn(PPtr)→发射口, AmmunitionObject(PPtr)→弹药模型(可0), EmptyLauncherCover(PPtr)→空发射筒盖(可0)}) |
## 防崩溃铁律与已知坑

### 数据库五条铁律（每条都由真实崩溃验证）

1. db.zip 必须含重复的 `PlaneFlyPreset.json`（无 s）—— 否则 LoadMobility 空引用崩溃。
2. 每个单位必须有 UnitPropulsions 行（LoadMobility 同样）。
3. 每个单位武器必须有 WeaponAmmunitions 行 —— 否则武器不可见并崩溃单位信息卡。
4. 单位的 UnitArmors 中至少一条 `IsDefault=True` —— 否则"未指定装甲包"不生成。
5. 载具雷达/APS 必须放在 ECM 能力内（ECM=1.0 是"幽灵"载体）；APS 缺 Hitbox 字段不拦截。

### 模型/炮塔侧坑

- preload 顺序：MonoScript 必须排在 MonoBehaviour 之前 —— 否则战场报
  `Read 32 bytes but expected 64 bytes` / `script unknown or not yet loaded`（军械库不受影响）。
- LODGroup 必须字节复制（typetree 写会漏可选字段 → 畸形序列化 → Unity 断点崩溃）；
  没有 LODGroup 场景里不显示。
- AnimationManagerBridge 的 _recoils 字典键必须是真实蒙皮骨骼；空物体壳点会导致
  战场 BoneContainer.Bake 崩溃（当前构建不挂 Bridge）。
- 切线 W 必须 -1（否则模型倒置/法线贴图错误）；绕序用有符号体积自动翻转。
- 换现有模型外形只能移动顶点（增删顶点/改拓扑 → 权重动画失效）；直接构造的新模型拓扑任意。
- 改完 bundle 必须重算 CRC 并写回 catalog，否则报 CRC Mismatch。
- 累积构建残留旧对象/旧 preload 条目 → 战场崩溃（军械库正常，极具迷惑性）；
  build_final.py 必须从纯净备份构建，build_turret_direct 有幂等清理兜底。
- MathConnect 的 WeaponShotForces 字典未完全破译 → 按字节透传（shotsRawB64），
  面板往返不得重建为最小形式（会丢字节 → 反序列化越界）。
- 传感器覆盖：Options.MainSensorId 覆盖单位直连的 SensorUnits；游戏视野米数 = 数据库值 × 2。
- 新单位不能用全新 Id：占原生槽位（Miller/飞行员/训练靶）克隆；Country 只能 1(RUS)/2(USA)。

### 排查流程

1. GameLogs\Gamelog__*.log 尾部（托管错误带完整类名堆栈）。
2. 错误模式对照：`Read 32 bytes but expected 64 bytes`=preload/脚本解析；`archive:/CAB-…
   is corrupted [Position out of bounds]`=对象序列化损坏；`CRC Mismatch`=catalog 未同步。
3. 军械库正常+战场崩 ⇒ LOD/BoneContainer.Bake/ECS 侧问题。

