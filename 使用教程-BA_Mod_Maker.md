# BA_Mod_Maker 使用教程（全功能）

> 版本：v1.8.11 · 配套 Blender 插件见《使用教程-Blender插件.md》
> 深层知识（数据结构/地址映射/常量/枚举释义/防崩溃铁律）见《数据库词典.md》与 Blender 插件内置词典。

BA_Mod_Maker = 断箭（Broken Arrow）模组制作的主工具：
编辑游戏数据库（24 张表）、导入模型/皮肤/姿势/图片 .bamod 包、自动 CRC、内置词典。

---

## 1. 安装与启动

- **免安装版**：直接运行 `BA_Mod_Maker_vX.Y.Z.exe`（发布文件夹里）。
- **源码版**：装 Python 后双击 `启动编辑器.bat`。
- 首次使用点 **文件 → 自动检测**相关按钮即可；游戏目录一般会自动找到
  （`<游戏安装目录>`）。

## 2. 核心概念（30 秒版）

- 游戏数据在 `BrokenArrow_Data\data.unity3d` 里的 **DataBaseCompiled** 资产（UABEA 路径 51978）。
- 24 张表（Units/Weapons/Ammunitions/Turrets/…）字段是 **AES 加密 JSON**。
- 改数据的两条路：
  1. **全自动**：文件 → 「从 data.unity3d 打开」→ 改 → 保存 → 「导入到 data.unity3d」。
  2. **UABEA 手动**：UABEA 导出 Dump → BA_Mod_Maker 打开 dump → 改 → 保存 → UABEA 导回。
- 模型/皮肤/姿势不在数据库里，而在 **bundle**（`units_assets_all_*.bundle`，3.4GB）——用
  Blender 插件打包 .bamod，再用本工具导入。

## 3. 文件菜单（全部功能）

| 菜单项 | 作用 |
|---|---|
| 打开 dump（Ctrl+O） | 打开 UABEA 导出的数据库 dump 文件（自动解密） |
| 打开文件夹（Ctrl+Shift+O） | 打开解密后的 JSON 表目录（如 `_decrypted/`） |
| 从 data.unity3d 打开 | 直接用内置 UABEADump 从游戏 data.unity3d 提取数据库（免手动 UABEA） |
| 导出到 data.unity3d | 生成可导回游戏的 dump 文件 |
| 导入到 data.unity3d | 用内置 UABEADump 把修改后的数据库写回 data.unity3d |
| **导入 .bamod 素材包…** | 模型包 / 皮肤包 / 姿势包（见 §6） |
| **导入图片/图标/肖像…** | 图片直导（见 §7） |
| **打包图标/肖像 (.bamod)…** | 只把图片打成 .bamod 不导入 |
| 保存 / 另存为 | 保存数据库（自动 AES 加密写回同名文件） |
| 导出文件夹 | 把当前数据库导出为明文 JSON 目录 |
| 退出 | — |

## 4. 编辑菜单与表格操作

- **撤销/重做**（Ctrl+Z / Ctrl+Y）、**加行**、**复制行**、**删行**（Del）。
- 左侧表格：24 张表切换（顶部标签或下拉）；单元格直接双击编辑，**改动即时自动提交**。
- 底部**搜索**（F3 快速定位）：按当前表字段过滤。
- **关联树**（右侧）：选中一行后显示该实体的全部关系（武器→弹药、单位→炮塔/成员/装甲…）。
  - 右键关联项：打开 / 复制行 / 删除 / **快速添加**（在关联表新建一行并自动绑定当前实体，
    如给单位加一个新小队成员、新炮塔、新武器）。
- **详情窗口**（双击行打开）：完整字段表单 + 顶部横幅 + **单位信息块**（载具/步兵/直升机/
  飞机的面板数值，含视野=Optics×2、隐蔽=1/Stealth、步兵重量=125×人数等公式悬浮提示）
  + 下方**迷你编辑器**（编辑关联行时按该行真实表查词典，显示中文字段名）。
- **私有拷贝**：改共享行会影响所有引用者；用「复制行 + 重绑引用」给当前单位做专属副本。

## 5. 工具菜单

- **查找（F3）**：全局搜索定位。
- **查找引用**：输入一个 Id/名字，找所有引用它的行（跨表）。
- **校验（F7）**：全库校验（5 条防崩溃铁律：PlaneFlyPreset 冗余、每单位必有
  UnitPropulsions、每武器必有 WeaponAmmunitions、UnitArmors 至少一条 IsDefault、
  雷达/APS 必须在 ECM 能力内），错误列表一键跳转。

## 6. 导入 .bamod 素材包（模型 / 皮肤 / 姿势）

对话框：选包 → 选目标 bundle（一般自动 = 游戏目录的 `units_assets_all_*.bundle`）
→ 可填**映射地址**（新模型注册 catalog 地址）→ 勾选 **CRC** → 开始导入。

- **模型包 / 姿势包**：Blender ③/⑥ 面板生成。导入 = 新对象写进 bundle + 容器条目 +
  preload + 重算 CRC。结果会显示对象数、新 root、CRC。
- **皮肤包**：Blender ⑤ 面板生成。导入 = 新建纹理 + 克隆材质换纹理槽 + 按皮肤 id 更新
  全部 SkinStorageBridge + CRC。结果显示「皮肤导入完成：皮肤 X · N 材质 / M 贴图」。
- **贴图替换包**：Blender ⑤+ 生成（枪械等无皮肤模型）。导入 = 新建纹理 + 克隆材质 +
  原地更新该模型渲染器的材质引用 + CRC。结果显示「模型贴图替换：N 材质 / M 贴图 / K 渲染器」。
- **姿势包**：新姿势模型导入后，还要在数据库里把 `SquadMembers.ModelFileName` 改成
  新模型名（§8.4）；原地替换模式则不用改数据库。

## 7. 导入图片/图标/肖像

文件 → 导入图片/图标/肖像…：
1. **图片类别**下拉（已汉化）：单位肖像 / 单位标签·缩略图 / 武器图标 / 弹药图标 / 国家·专精。
2. 添加图片（可多选）→ 每行自动生成**容器路径**（bundle 内路径）与**映射地址**
   （catalog 地址），双击行或「编辑选中」可自定义（路径/地址都是像导入模型一样注册）。
3. 勾选：注册映射地址 + 计算 CRC；也可「只打包 .bamod」。
4. 开始导入 → 按类别自动分组写进对应图片 bundle，完成后游戏即可引用新地址。

## 8. 四个标准工作流

### 8.1 改数值（最简单）
1. UABEA 打开 `data.unity3d` → 找到 DataBaseCompiled → Export Dump（导出 txt/json）。
2. BA_Mod_Maker 打开 dump → 改字段 → 保存（自动加密）。
3. UABEA Import Dump → 保存 data.unity3d → 进游戏验证。
（或用文件菜单「从 data.unity3d 打开 / 导入到 data.unity3d」全自动。）

### 8.2 换/改模型（挂点+网格）
1. Blender ① 导入原模型 → ② 编辑挂载点 → 改网格 → ③ 构建（copy-full）→ .bamod。
2. BA_Mod_Maker 导入 .bamod（勾 CRC；新模型填映射地址）。
3. 数据库里把单位的 `ModelFileName`（或 `SquadMembers.ModelFileName`）改成新地址名。

### 8.3 皮肤重涂
1. Blender ⑤ 皮肤：导入炮塔模型 → 扫描 → 应用 → 导出贴图 → 改 PNG → 打包 .bamod。
2. BA_Mod_Maker 导入 .bamod（皮肤包）。游戏菜单里该皮肤槽即显示新涂装。
（新增皮肤槽模式需另改 data.unity3d 的 UserItemsConfig，暂未工具化——用重涂现有槽最稳。）

### 8.4 步兵姿势
1. Blender ⑥ 摆姿势 → 打包姿势模型 .bamod（新路径或原地替换）。
2. BA_Mod_Maker 导入 .bamod。
3. 新增模型：数据库 `SquadMembers` 表把对应成员的 `ModelFileName` 改为新模型名 + 注册地址。

### 8.5 枪械/武器贴图替换
1. Blender ① 导入枪械 → ⑤ 默认材质 → 导出贴图 → 改 PNG → 打包贴图替换包。
2. BA_Mod_Maker 导入 .bamod（贴图替换包）→ 游戏里该枪立刻换新贴图（无需改数据库）。

### 8.6 音频/音效导入
1. 文件 → **导入音频/音效** → 列出游戏全部 FMOD 音库（*.bank + .gts/.gtp）。
2. 替换选中：选新音频文件覆盖同名音库（首次自动备份到 `_ba_audio_backup\`）→ 进游戏生效。
3. 批量替换：选文件夹按文件名匹配覆盖；还原选中：一键回滚备份。
4. 说明：断箭音效在 FMOD Studio 音库（无 CRC 校验，整文件替换即生效）。

### 8.7 添加音频/音效（新音效，实验性）
1. 文件 → **添加音频/音效（新音效）**（或音效对话框的「添加新音效」按钮）。
2. 选音频文件（WAV 直读；mp3/ogg 需 ffmpeg.exe 在 PATH 或工具目录）→ 自动生成
   事件 `event:/BrokenArrowGame/BA_Mod/<名>`。
3. 「添加音效」→ 注入 CustomDialog.bank（空音库，启动即加载）：新事件 + 波形 +
   PCM16 样本（自动加密）+ 总线路由 + HASH 表；原文件自动备份 `.bak`。
4. 可选勾选「同时创建武器音效预设」：填预设名（如 `WEAPON_SOUND_PRESET_MY_GUN`）
   → 克隆预设资产注入 sfx bundle + catalog 注册 + CRC → 在数据库编辑器把武器行的
   `AudioPreset` 改成该预设名。
5. **实验性说明**：事件路径→GUID 的字符串表（Master.strings.bank）注册尚未完成——
   游戏按名解析新事件依赖它。进游戏实测：若音效不响，看 `GameLogs` 的 FMOD 报错
   并反馈；strings 表注册与事件↔样本链接字段在后续版本补齐。还原：删掉
   `CustomDialog.bank`，把 `CustomDialog.bank.bak` 改回原名。

## 9. 词典与语言

- 主界面「词典」按钮 → 内置词典窗口（24 表字段释义/枚举/公式，随界面语言切换）。
- 《数据库词典.md》随发布目录分发，是唯一的知识载体（数据结构、地址映射、常量、铁律）。
- 菜单「语言」：中文 / English / Русский 即时切换。

### 9.1 绿色箭头 →（枚举定位 / 外键跳转，v1.8.55 起；v1.8.56 起字形统一）

单位大类 `Type`、槽位类别 `CategoryType`、角色 `Role`（以及武器类型、弹道类型 `TrajectoryType`、
`TargetType` 等所有枚举字段）在字段编辑器里，**右侧会显示绿色箭头 + 该 ID 对应的种类**。
外键字段（`UnitId`/`WeaponId`/`ArmorId`…）的 `→ 名称` 与枚举箭头现在是**同一个字形、同一个颜色、
同一个位置**：

```
角色（战术兵种）  [ 11 ]    → 主战坦克 (Tank)              ← 绿色
角色（战术兵种）  [ 999 ]   → ⚠ 未知值 999（游戏按兜底分支处理）  ← 黄色（不会崩）
外键 UnitId      [ 1 ]     → T-72B                       ← 绿色，点击跳到该行
```

- 改数字时**实时更新**；鼠标悬停箭头可看该枚举的**全部取值表 + 「真实作用」说明**。
- **点箭头或它的文字** → 枚举：打开词典窗口并定位到该枚举对照表；外键：跳到被引用的那一行
  （与外键行、关联树双击的行为一致）。
- 主表格里 `Units` 有 `Type / CategoryType / Role` 三列，`Ammunitions` 有 `TargetType / TrajectoryType`，
  `Weapons` 有 `Type`，单元格直接写成 `4 → 载具`、`11 → 主战坦克 (Tank)`，不用再对照数字。

#### 位掩码字段：按加法拆解（v1.8.56）

`Ammunitions.TargetType`（弹药能打什么）和 `Units.Type` 是**位掩码，值按位相加**：

| 位 | 含义 | 位 | 含义 |
|---|---|---|---|
| 1 | 地面 | 32 | 舰船 |
| 2 | 步兵 | 128 | 弹丸 |
| 4 | 载具 | 256 | 反辐射导弹(SEAD) |
| 8 | 直升机 | 512 | 巡航导弹 |
| 16 | 飞机 | 1024 | 弹道导弹 |

例：**36 = 32(舰船) + 4(载具)** → 该弹药能打舰船和载具；39 = 32+4+2+1（舰船+载具+步兵+地面）；
1944 = 1024+512+256+128+16+8。工具会自动拆解并在字段右侧/表格里写出
`36 → 舰船 + 载具 (32 + 4)`；**64 这一位游戏没有定义**，填了会标黄提示 `未知位 64`。

#### 三者分工（别改错字段）

- `Type` = 引擎大类（步兵/载具/直升机/飞机/舰船…，位掩码）
- `CategoryType` = 卡组槽位类别（侦察/步兵/战斗/支援/后勤/直升机/飞机）
- `Role` = 战术角色（决定主武器判定、AI 火力任务目标、战报勋章归类）

克隆单位换定位时**一定要同步改 Role**，否则主武器空弹提示与 AI 行为会错位。

## 10. 常见问题

- **找不到 bundle**：文件菜单导入 .bamod 时手动选 `…\StreamingAssets\aa\PC\units_assets_all_*.bundle`。
- **CRC 太久**：3.4GB bundle 每次 CRC 约 1-2 分钟，正常；不勾 CRC 进游戏会报
  `CRC Mismatch`（日志里的 calculated 值可手动填回 catalog）。
- **导入报「找不到类型」**：目标 bundle 版本不对（游戏更新后需重新构建包）。
- **修改后游戏崩溃**：先跑 F7 校验五条铁律；游戏日志 `GameLogs\*.log` 有具体报错。
