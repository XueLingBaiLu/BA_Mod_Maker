# Blender 插件使用教程（全功能 · BA Mod）

> 版本：v2.12.9 · 面板都在 3D 视图侧边栏（N 键）的「BA Mod」标签页。
> 深层知识（数据结构/挂载点/组件/动画/皮肤/地址与常量）见《Blender工具词典.md》
> 与《数据库词典.md》。
> 配套主工具见《使用教程-BA_Mod_Maker.md》。

---

## 0. 安装与偏好设置

1. **装插件**：Blender 偏好 → 插件 → 安装 → 选 `BA_Mod_Maker_blender_addon.zip` → 启用
   「BA Mod Maker（断箭模型工具）」。
   （或运行《安装Blender插件.py》：自动装 UnityPy + 解压启用 + 自动检测路径。）
2. **偏好设置**（插件名右侧下拉 → 偏好）：
   - 游戏 bundle 文件：点「自动检测并填充路径」（自动找 Steam 游戏目录与 units bundle）。
   - 输出 bundle / 新 prefab 路径 / 新名字：构建写回用。
   - UnityPy / _rev_tools 目录：一般不用动（插件自带）。
3. 3D 视图按 **N** → 侧边栏「BA Mod」→ 八个面板（① 模型导入 / ② 挂载点 / ③ 工具 / ④ 动画 /
   ⑤ 皮肤 / ⑥ 涂装 / ⑦ 模型自带动画 / ⑧ 组件字段）—— 本教程 ①~⑧ 全覆盖。

## ① 模型导入

1. 点「刷新 prefab 列表」（读 3.4GB bundle，约 1 分钟，之后列表可搜索）。
2. 搜索框输入关键词（如 `RU_T90A`、`US_MTVR`、`Motostrelki_rifle`）。
3. 选中 → 「导入所选模型」：
   - 挂载点/骨骼 = 空物体（Empty）层级；网格 = 带顶点组的蒙皮网格；UV 自动导入。
   - **步兵模型自动创建 Armature 骨骼 + 骨骼修改器**（姿势实时变形用）。
   - 「只导入 LOD0」默认开启（只看最高精度）。

## ② 挂载点

- 「刷新挂载点列表」→ 列出场景全部挂载点（名称/分类/层级）。
- 「选中/定位」：列表点选 → 视口定位到该空物体。
- 「添加挂载点」：在选中物体（或光标）处新建；填名字后自动按命名规律分类。
- 「删除选中挂载点」：列表选中后删除。
- 挂载点直接改：移动/旋转空物体即可（导入的空物体是 QUATERNION 模式，四元数无损）。
- 挂载点分类/命名规律详见《Blender工具词典.md》第 2 节。

## ③ 工具（构建写回 / 权重 / CRC）

- **构建写回**（把场景写回 prefab）：
  - `copy-full 完整复制`（推荐）：整段字节复制原 prefab，只 patch 网格+挂载点，材质/动画/
    皮肤桥原样保留。
  - `打包 .bamod`（推荐）→ 生成素材包，用 BA_Mod_Maker 导入游戏；不勾则直接写完整
    .bundle（大文件，慎用）。
  - 新 prefab 路径 / 新名字：填了就建新模型，留空=用原路径（原地替换）。
- **权重转移**：选中你的新网格 → 用场景里参考网格（顶点组最多的）做 KD-tree 最近邻转移
  蒙皮权重。
- **计算 CRC**：把构建产物放到游戏目录的 units bundle 后点它 → 重算 CRC 写回 catalog
  （1-2 分钟）。

## ④ 动画

- 「读取动画」：从当前模型读 AnimationHub 行为列表（每个行为一个**折叠栏**，默认折叠）：
  - 标题 = 序号 · 数组（universal/demo/game/preDeath/death）· 类型 · 摘要
    （AxisRandom 显示各轴源与角度范围；MathConnect 显示 根→目标）。
  - 点箭头展开编辑字段（源/角度/频率/阻尼/冻结轴…）；MathConnect 含
    **WeaponShotForces 力度字典**（骨骼→力度结构化显示，未破译类 rawB64 透传无损）。
- 「+ 随机扫掠」「+ 跟随」：新增行为；X 删除。
- 内容同步到文本块 `bamod_anim`（可手改 JSON）。
- 勾选偏好「构建时应用动画」→ ③ copy-full 构建时把编辑后的动画写进新 prefab。

## ⑤ 皮肤（载具涂装）

1. **导入正确的模型**：皮肤的桥都在**炮塔/士兵 prefab** 上——坦克导入炮塔变体
   （如 `RU_T90A`、`RU_T80U`；炮塔 prefab 自带车体网格），步兵导入士兵成员模型。
2. 「扫描皮肤」→ 列表出现可用皮肤（同族多个桥同 id 自动合并；扫不到会提示
   「请导入同名炮塔变体」）。
3. 选中皮肤 → 「应用皮肤」→ 视口实时换材质+贴图；「默认材质」还原对比。
   - **视口着色**：材质预览（Z → Material Preview）看完整贴图；实体模式也显示
     贴图平均色（材质视口色自动设置）。
   - 皮肤不覆盖的部件自动回退默认材质，整车外观完整。
4. **改皮肤**：「导出贴图」→ PNG 到目录 → PS/纹理绘制改图（文件名不变）→
   放回同一目录 → 填输出路径 → 「打包皮肤 .bamod」。
   - 新增皮肤 Id 填 0 = 重涂原槽（游戏菜单名不变，推荐）；填新 Id = 复制为新槽
     （菜单显示需另改 UserItemsConfig）。
5. BA_Mod_Maker 导入该 .bamod → 进游戏选对应皮肤看效果。

### ⑤+ 枪械/无皮肤模型的贴图替换（v2.7.12）

游戏本身**不给枪械做皮肤**（皮肤桥只存在于炮塔/步兵 prefab，268 个武器 prefab 无一被桥覆盖），
所以枪械改外观走「贴图替换包」，任何时候都生效：

1. ① 导入枪械/炸弹等模型（如 `FAB250_2_Long`）。
2. ⑤ 「默认材质」→ 材质预览看当前贴图。
3. 「导出贴图」——**没有应用皮肤时自动导出该模型的默认材质贴图**
   （全部纹理槽：Albedo/Mask/Normal 等）。
4. 改 PNG → 放回同一目录。
5. 填「替换包 .bamod」路径 → 「打包贴图替换包」。
6. BA_Mod_Maker 导入 → 该模型（此 prefab 的渲染器）直接换新贴图，游戏里立刻可见。

### ⑤++ 只给「选中的那几个网格」换材质 —— 移植过来的零件用它原机贴图（v2.10.0）

**场景**：把 B 车的零件装到 A 车上（例如给飞行 ACV 装上 AH-1Z 的旋翼）。零件用的是 A 车的材质，
UV 跟 A 车的贴图对不上 ⇒ 满身乱斑。这个入口就是"把零件指回它**原机**的贴图"。

面板位置：**⑤ 皮肤 → 打包 → 「枪械/无皮肤模型」那块**。

1. **准备贴图（两条路，选一条）**
   - **PNG 内嵌**：① 先导入**来源机**（如 AH-1Z）→ ⑤「导出贴图」拿到它的图；再导入**目标模型**
     （如 ACV）→ ⑤「导出贴图」拿到**槽名对应的 PNG 文件名**（如 `US_ACV_1_BaseMap.png`）；
     把来源机的图**改名成目标槽的贴图名**放进同一个「贴图目录」。
   - **流式引用**（更省体积，推荐）：**不用改图**。面板里「贴图来源」选 **流式引用（原机贴图，包最小）**
     → 在「搜索」里填贴图名的一部分（如 `AH-1Z_viper`）→ 「列出候选贴图」→ 在列表里点一条。
     ⇒ 包里只多几百字节（实测同一张 2048² 贴图：流式 **532 字节** vs PNG 内嵌 **1.96 MB**）。
     - 每一行后面会写 **`·引用N`** = 这张贴图被几个材质用着。**`·引用0`（前面是红叹号）的要当心**：
       它可能是一张**没人用的死数据**；选它进游戏多半看不到图，换一张有引用的。
       （⛔ 别用"图能不能解出来"当判据 —— 解不出来往往只是读到了别的副本的数据，不代表这张没用。）
2. **在视口里选中要换材质的网格**（例如旋翼），勾上 **「只对选中对象生效（不勾=整车）」**。
   面板会显示"已导入网格 N 个 / 选中 M 个 ⇒ 只换选中的那 M 个"，**点之前就看清后果**。
3. 填「替换包 .bamod」路径 → 「打包贴图替换包」→ BA_Mod_Maker 导入 → ④ 收尾同步 CRC/自检/安装。

⛔ 三个容易踩的点：
- **不勾 = 整车一起换**：想让车身保持原样，**必须勾上并选中零件网格**；勾了却什么都没选会**明确报错**，
  不会偷偷按整车来。
- 「槽名」留空 = 自动挑**基准色槽**（贴图名带 BaseMap/BaseColor/Albedo 的那个）；法线/粗糙度槽不会被误换。
  自动挑不中时会告诉你在「槽名」里手工填。
- 流式引用用的是**游戏里已有的**贴图，所以贴图名要能在包里搜到；搜不到就用 PNG 那条路。

### ⑤+++ 「打包贴图替换包」的两种范围 + 覆盖面（★⑯ / ★⑰）

> 这一节是 ⑤+ / ⑤++ 共用的规则：**同一个按钮「打包贴图替换包」**，范围由那个勾选框决定。

#### 一、两种范围：勾不勾「只对选中对象生效（不勾=整车）」

面板上那个勾选框下面**实时**写着一行后果，**点打包之前就能看清**：

| 勾选状态 | 换哪些渲染器 | 面板上看到的那行字 |
|---|---|---|
| **不勾 = 整车**（默认） | 场景里**全部**已导入网格 **+** prefab 子树里能认出来的**构建期渲染器**（见下节） | 「已导入网格 N 个 / 选中 M 个 ⇒ **整车 N 个一起换**」 |
| **勾上 = 只对选中生效** | **只在视口里选中的**那些网格（移植零件的正解） | 「已导入网格 N 个 / 选中 M 个 ⇒ **只换选中的那 M 个**」 |

- ⛔ 勾了却**一个都没选** ⇒ 打包**直接报错中止**，原文：
  「勾了「只对选中对象生效」，但视口里没选中任何已导入的网格 —— 先在视口里选中要换材质的网格
  （例如旋翼），或者取消勾选（=整车一起换）」——**不会偷偷按整车来**。
- 典型用法：给装了 AH-1Z 旋翼的 ACV 换贴图 ⇒ **勾上 + 只选旋翼** ⇒ 车身一个渲染器都不动。

#### 二、覆盖面边界（★⑰）：整车模式到底盖到哪、哪些盖不到

**问题**：以前的「整车」只覆盖**当前 `.blend` 场景里存在的网格**（实测那台飞行 ACV 只有 **3** 个：
`Chassis` / `BaseArmor` / `Ah_1z`），而 prefab 子树里还有 **5** 个**构建期生成的渲染器**（各级 LOD 的
skinned 克隆等）**不在场景里** ⇒ 它们保持原材质 ⇒ 用户**拉远 / 切到低 LOD 时又看到旧贴图**，
看着像"没换干净"。

**现在**：不勾（整车）时会去读 prefab **子树**里的全部渲染器，但**只认两条硬证据**才归属：

- **Tier A**：渲染器 GameObject 名 == 你的网格名（忽略 Blender 加的 `.001`；忽略前导序号与 `skinned` 词
  ⇒ `000_skinned_Chassis` 也算 `Chassis`）；
- **Tier B**：它的 **Mesh 资产名**去掉前导序号后 == 你的网格名（实测 `02_Chassis` / `03_Chassis` → `Chassis`）。

**认不出来的，一律不动**，并在日志里**逐条写明原因**（⛔ 不猜：猜错 = 把 A 部件的图贴到 B 部件上，
比"没换"更难发现）：

- **只共享词元**的：实测 `Armored` 的 mesh 叫 `Uparmored`，只和 `BaseArmor` 共享 `armor` 几个字母；
- **没有 `m_Mesh` 的**：实测 `lod_1x1x1` 是 `MeshRenderer` 但 `m_Mesh=None`（占位/LOD 代理，本身不画东西）。

⇒ **所以边界是**：这几类渲染器**仍然保持旧贴图** ⇒ **拉远 / 切到低 LOD 时，那一小部分可能还是旧图**。
这不是 bug，是"**判不出归属就不动**"的取舍。

**怎么核对它到底换了哪些**（打包后看 Blender 的 Window → Toggle System Console）：

```
[★⑰] 整车覆盖面：prefab 子树 8 个渲染器 ⇒ 归属 6、按保守规则不动 2
   ✓ 会换：Chassis                  ← Chassis（Tier A：渲染器名与网格名相同）
   ✓ 会换：000_skinned_Chassis      ← Chassis（Tier A：渲染器名与网格名相同）
   · 不动：lod_1x1x1               ← 没有 m_Mesh（占位/LOD 代理，本身不画东西）
   · 不动：Armored                 ← 只共享词元（Uparmored ⊃ BaseArmor）⇒ 归属不清
```

打包完成那行也会写清楚：「已打包 …（N 材质 / M 贴图 / K 渲染器，**整车（含 prefab 子树里构建期生成的
A 个）**）」。想连"判不出归属"的那几个也换 ⇒ 把那些零件**也导入到场景里**再打包
（① 导入模型；必要时到插件偏好里关掉「只导入 LOD0」，否则它们可能被过滤掉、你在视口里根本选不到）。

⚠ 子树展开失败（包不存在 / prefab 名对不上等）**不会让建包失败**：会打印一行 ⚠ 并**退回旧行为**
（只改场景里的网格），建包照常完成。

#### 三、⛔ 导入到「我的 bundle（自建包）」**必须用新版打包**（v2.11.0+，★⑯）

- 新版打的包里**多带名字**：`renderer_info[{pid, go, mats}]`、`mats[].orig_name`
  （渲染器 = GameObject 名；材质 = 原材质名）。导入端**先按 pid、查不到再按名字**定位。
- **为什么必须**：`copy_full` 建出来的**自建包会把 pid 全部重新分配** ⇒ 旧包里只有 pid，
  在目标包里**一个都查不到** ⇒ 导入直接中止并报
  `ValueError: 找不到原材质 pid …`（旧包在**自建包**上必失败）。
- ⚠ 旧包**不是全废**：目标 = **游戏自带的 units bundle**（源包 == 目标包）时行为**一字不变**；
  只有导入到**自建包**才必须用新版重新打包。
- 报错时它还会给三段**可执行**指引：① 用新版插件重新打包 ② 确认目标包里确实有这个零件
  ③ **网格名要与目标包里 GameObject 名一致**。

## ⑥ 涂装（在车上画 / 贴图片）

> ⚠ **本节已按现行面板改名**：这里原来写的是「⑥ 步兵姿势」——该面板（`BAMOD_PT_Pose*`）已在
> **v1.8.52 整段下线**（源码注释原文：「姿势本质是动画，改用 ④ 里的『导入动画到时间轴』」）。
> 现行编号是 **⑥ 涂装**、**⑦ 模型自带动画（防浪板 / 舱门 / 折叠）**、**⑧ 组件字段（其它组件）**
> —— ⑦ / ⑧ 的操作步骤在本节后面（见「⑦ 模型自带动画」「⑧ 组件字段」两节）。

1. ⑥ 主栏写着流程：**① 开始涂装 → ② 画 / ③ 贴图 → ⑤ 保存 → 打包**。
   先填「贴图目录」→ 点 **① 开始涂装（导出贴图 + 建绘制材质）**。
2. 点 **② 打开画板（在图像编辑器里涂）** → 在图像编辑器里用左侧「纹理绘制」+ 任意笔刷直接画。
3. 想贴图片：展开子栏 **③ 贴图片到车上（盖印 / 整张铺满）** → 选「图片」→ 调左右 / 上下 / 宽度 /
   旋转 / 不透明度 / 羽化 → 点 **③ 贴图片到车上（盖印）**
   （**改参数再点 = 重新盖一次**，可撤销；勾「整张铺满（换皮）」= 整张替换而不是盖印）。
4. 用外部软件画（Photoshop / Krita）：展开 **④ 外部软件 / 保存 / 打包** → 改完点
   **③ 从文件重新载入（外部软件画完用这个）** → 点 **④ 保存涂装到贴图目录**。
5. 打包：同一个子栏里的 **打包贴图替换包**（输出路径 = 该子栏的「输出」）⇒ 生成的 `.bamod`
   用 BA_Mod_Maker 导入（导入链见 ⑤+；范围与覆盖面见 ⑤+++）。
6. 想还原：展开 **⑤ 还原原始贴图（会先备份）** → 当前涂装会先备份成 `*.painted.png`。

## ⑦ 模型自带动画（防浪板 / 舱门 / 折叠）

**管什么**：模型**自带的 Unity AnimationClip**（防浪板 / 舱门 / 折叠 / 装卸载…）——游戏里由 Trigger 播，
这里把它**列出来 + 打成关键帧**在 Blender 里看。⛔ 不是 ④ 的 AnimationHub「动画行为」（那是引擎侧的行为数组）。

1. **先在 ① 面板选中模型**（⑦ 主栏没列出片段时会写「先在 ① 面板选中模型，再点上面那个」）。
2. 点 **① 列出模型自带的动画片段**（第一次要读 bundle，稍等；之后有缓存）。
3. 在「片段」下拉里挑一条 → 点 **② 导入成关键帧（拖时间轴看它动）**
   （旋转/位移都按片段里的**真实数值**；**时间戳解不出**时关键帧按等间隔铺满真实时长）
   → 导入后拖时间轴即可看到它怎么动。
4. 展开子栏 **「节点位置 / 时间轴选项 / 说明」**：
   - **③ 查这个零件在车的哪儿（世界坐标）** = 把片段里动的节点连世界坐标一起打印（判断装车头/车尾/车顶）；
   - **时间轴帧率**（默认 24，1~120）：片段时长 × 帧率 = 帧数；
   - **驱动范围**（提示写着「接缝被拉开时改这里」）三选一：
     「**全部按片段（和游戏一致）**」= 默认最忠实；「**只驱动主件（子件刚性跟随）**」= 修"连接杆看起来断开"
     （代价：子件过场动作与游戏不同）；「**跳过指定节点（下面填名字）**」+「跳过的节点」= 逗号分隔节点名
     （例：`Shield_01,Shield_help`）。

## ⑧ 组件字段（其它组件）

**管什么**：prefab 上**其它 MonoBehaviour** 的字段（④ 只管 AnimationHub 的动画行为）。
实测 `US_ACV` 上就有 `AnimationManager._container._settings` 的 `WheelSpeed` / `SuspensionSpeed` /
`BodyShake`（车身抖动）/ `BodyMass`（车速与抖动的真实来源），以及 `UnitPrefabTurretInfo` /
`FmodTurretsTurn` / `SkinStorageBridge` / `EffectsSpawnPoint` 等。**判定标准**：写回后与原字节
**完全一致**才算"可编辑"；不一致的只列出来并标红说明为什么，⛔ 绝不假装能改。

1. **【前置】先 ① 导入模型**（它会记录「复制源」）—— ⑧ 顶部显示「**复制源：<名字>**」，
   没导入时是红字「（还没 ① 导入）」。
2. 点 **「扫描组件」**（左边放大镜输入框 = 过滤，按**类名**筛）→ 面板按节点列出所有能改的组件。
3. 在「组件」下拉里挑一个 → 面板显示 **节点 / 类 / 字节数 / 字段数**，并给两种状态行：
   - 「该组件**没有可编辑字段**（整段字节原样保留）」= 只读；
   - 「（**末尾 N 字节无对应字段**，会原样保留）」= 这部分字节原样写回；
   - 选中 `AnimationHub` 时提示：「AnimationHub 的动画行为请用 ④ 面板（这里只显示字段数）」。
4. 点 **「读取字段」** → 展开子栏 **「通用字段（全部字段都在这里）」**（和 ④ 用的是**同一套控件**）改值
   → 点 **「写回模型」**：把面板字段写成字节并**当场校验**（面板→字节→回读），存进"待构建的改动"
   （**构建时**才写进模型）。状态行会显示「面板上有未写回的改动」/「**已写回 ✓（构建时生效）**」；
   旁边的回转箭头 = **「撤销写回」**（把该组件从待构建清单里去掉）。
5. 子栏 **「已写回的组件」** 列出全部待构建改动（`[节点] 类 · N 字段`）→ **「清空全部组件改动」** 一键丢弃。

⚠ 两个前置/踩坑：
- 报「复制源的组件清单是空的 —— 偏好里的 bundle 指对了吗？」⇒ 回插件偏好检查 bundle 路径。
- 报「复制源里已经没有 pid=… 这个组件了（模型被重新导入过？）」⇒ **模型重新导入后组件 pid 会变**，
  点一次 **「扫描组件」** 重新扫。
- ⑧ 改的是**「复制源 prefab」**（不是场景里的网格），改完照样要 **③ 构建写回** 才落进 `.bamod`。

## 完整工作流速查

| 目标 | 流程 |
|---|---|
| 改数值 | 不进 Blender：UABEA 导出 dump → BA_Mod_Maker 编辑 → 导回 |
| 改模型/挂点 | ① 导入 → ② 挂点 → ③ copy-full 打包 → BA_Mod_Maker 导入 → 改 DB 引用 |
| 换皮肤 | ① 导入炮塔 → ⑤ 扫描/应用 → 导出贴图改图 → 打包 → BA_Mod_Maker 导入 |
| 换零件贴图（移植件） | ① 导入 → ⑤ 导出贴图/列候选贴图 → 勾「只对选中对象生效」选零件 → 打包替换包 → BA_Mod_Maker 导入 |
| 涂装（在车上画） | ⑥ 开始涂装 → 画板涂 / 贴图片 → 保存 → 打包替换包 → BA_Mod_Maker 导入 |
| 看模型自带动画 | ① 导入 → ⑦ 列出片段 → 导入成关键帧 → 拖时间轴 |
| 改其它组件字段 | ① 导入 → ⑧ 扫描组件 → 选组件 → 读取字段 → 改 → 写回模型 → ③ 构建写回 |
| 换图标/肖像 | 不进 Blender：BA_Mod_Maker 文件 → 导入图片/图标/肖像 |

## 常见问题

- **扫描不到皮肤**：导入的是车体/单位锚点；换同名**炮塔变体**导入（面板会提示建议名）。
- **皮肤灰/白**：材质预览模式看贴图；实体模式看平均色。中文界面节点已按类型定位，
  如仍异常重新点一次「应用皮肤」。
- **导入报 OverflowError**：旧版 bug，已修复（pid 字符串存储）——更新插件即可。
- **Blender 装不了插件**：bl_info 已改为纯字面量（v2.7.4+），用新 zip 安装。
- **整车换了贴图，拉远 / 切低 LOD 还是旧图**：prefab 子树里那几个**判不出归属**的构建期渲染器
  没被换（只共享词元 / 没有 `m_Mesh`）——按 ⑤+++ 第二节核对打包日志；要连它们一起换就把那些零件
  也导入场景再打包。
- **贴图替换包导入报 `找不到原材质 pid …`**：包是旧版插件打的（或目标是自建包）⇒ 用 v2.11.0+
  **重新打包**（见 ⑤+++ 第三节）。
- **导入模型后找不到某个网格**：「只导入 LOD0」把它过滤了——导入完成时面板会**报警并点名**，
  到插件偏好里关掉「只导入 LOD0」再导入。
- **车辆网格不随挂载点动**：正常——车辆骨骼是挂载点语义。
- **导入很慢**：bundle 3.4GB，首次读列表/导入各需 1 分钟上下，属正常。

### 位置/摆放：怎么保证「Blender 里看到的 = 游戏里实际的位置」（★25）

- **一键「一致性自检」**（② 挂载点面板，和「摊平父级逆变换」**并排**）：逐对象比 **视图位置** 与
  **导出器会写进产物的位置**（＝从根沿父链连乘 `loc/rot/scale`）。**全绿 ⇒ 视图与写回一致**；
  有红就**点名**那个对象与偏差（m）⇒ 选中它点「摊平父级逆变换」（**只清父级逆变换、不改 loc
  ⇒ 导出结果不变**），再自检一次即可全绿。
- ⛔ **约束驱动节点 / 动画行为：Blender 侧不复现** —— 例：包内那些 `AimConstraint`／`RotationConstraint`
  在游戏里由引擎求解最终姿态，`AnimationHub` 的行为同理 ⇒ **别拿 Blender 里的静态位置推断游戏里的
  最终姿态**，这类节点的最终位置/朝向要看实机。**本工具不承诺在 Blender 里复现它们。**

- **怎么确认"导入时折平"真的跑过（两条凭据，任一即可）**
  1. 点面板「**一致性自检（视图 = 将写回的值？）**」⇒ 报**全绿**即表示：**视图位置与导出器将写回的位置一致**。
  2. 打开 Python 控制台执行 `bpy.context.scene["ba_import_normalize"]` ⇒ 得到 `{checked, fixed, fixed_names, moved_m, skipped}`：
     * `checked` ＝ 本次**清点的对象数**（**分母** —— 别看只看 `fixed`）；
     * `fixed` ＝ 实际**折平**的对象数（附 `fixed_names` 与每处**视图位移米数** `moved_m`）；
     * `skipped` ＝ **跳过**的（两种原因：祖先带**非均匀缩放**；或折平后**数值对不上、已回滚**）。
     ⇒ **`checked` 有值、`fixed` 与 `skipped` 都是空** ⇒ 这份模型**本来就没有需要折平的对象**（正常）。

### 历史条目（面板已下线）

- 原 **⑥ 步兵姿势**（滑条摆姿势 / 姿势库 / 「从游戏模型读取姿势」/「打包姿势模型」）与
  「摆姿势网格不动」「『读取姿势』后姿势没变」这两条问答，对应的面板已在 **v1.8.52 整段下线**
  （姿势本质是动画 ⇒ 改用 ④ 动画）；**保留在此仅作历史记录**，现行面板里没有这些按钮。
